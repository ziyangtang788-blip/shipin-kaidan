# 食品配送开单系统 — 工作规矩

> 这个文件每次开会话都会整份读进上下文，所以**必须短**。
> 中途改它会让缓存前缀失效、整段重算，要改就下次会话开头改。

## 一、省钱铁律

### 输出
- 任何表格/整理结果一律写进文件（.xlsx 或 .csv），**绝不在对话里打印整张表**。
- 回复只给三样：**文件路径、行×列数、异常项**（哪几格有问题）。
- 禁止复述表格内容、禁止回显我贴进来的原文、禁止客套小结。一句说清就一句。

### 认表（读取）
- 先用 markitdown 看结构，只读表名+表头来定位；要哪列就用 openpyxl/pandas 精确读那几列/那几格。
- 绝不为了回答「用第几列」把整张 sheet 读进上下文。

### 读图
- 照片/PDF 用代码 OCR/解析，结果直接写进 .xlsx，只回「已写入 N 行 × M 列 → 路径」。

### 回头核对
- 用代码比对两份数据，只输出对不上的那几格（位置 + 两边的值），不重贴任何一份原文。

### 缓存纪律
- 一次会话里不要中途换模型、不要改本文件——两者都会让上下文重算、白花钱。
- 认表这类活开 Sonnet 会话；读图/核对这类零容错的活开 Opus 会话。分开开，别混着切。

### 批量历史单
- 历史回填不要在交互里一条条跑。生成一个走 Batch API 的脚本（输入输出各五折、24h 异步），
  **我确认后再执行**。

## 二、这个项目的死规矩

- **认错必须为 0**。存疑不用管，但读错一个数字就是事故。不许拿「跟以前持平」交差，弹窗默认值也得是对的。
- **匹配逻辑只准一份**。抄第二份会出现「页面死了体检却全绿」。上传前必须跑 `node 全部检查.js`。
- **反常的拎出来问，别改规则**。单条特例不许动全局规则。
- **斤转板**：整除优先，除不尽按斤。跟观麦冲突时以这条为准。
- **改完要部署**：本地改完双击 `桌面\部署包\2-上传网页.bat` 才生效（部署包不在项目文件夹里）。
- **密钥只在 `密钥-本机.js` 和服务器上**，绝不进任何上传的文件。

## 三、要查细节去哪（这里只放结论，别把细节抄回来）

`说明-规矩总表.md` 是**唯一权威**，523 行，不进上下文——按下面的节号只读那一段：

| 要查什么 | 去哪 |
|---|---|
| 数量从哪来、一行几个数字、点位四种写法、名字留哪些字 | 规矩总表 §一 |
| 7 道闸门顺序、什么时候敢拍板、单价反推 | 规矩总表 §二 |
| 斤转板的实例和已知冲突点 | 规矩总表 §三 |
| 什么时候学、同词多行、忘掉重学、存哪 | 规矩总表 §四 |
| 加单/改单、押金、重复图、数字看不清写 `?` | 规矩总表 §五、§五点七 |
| 格子表怎么读、五道校验、Excel 只认表头 | 规矩总表 §五点五 |
| 点位册 | 规矩总表 §五点六 |
| 上线步骤、域名账号 | 规矩总表 §六 |
| 还欠着什么 | 规矩总表 §七 |

老板拍板的**原话和为什么**在 `记忆/`（索引 `记忆/MEMORY.md`）。三者对不上，以他最近那句为准。

## 四、目录里的东西

- `配送开单台.html` — 主页面（单文件，25 万字）
- `引擎-*.js` — 解析/匹配/读表/认表/点位册
- `后端-server.js` — 服务器（订单记录、单号、`/api/ocr` 代理）
- `测试-*.js` / `体检-*.js` — 回归；`全部检查.js` 一次跑完
- `工具-批量跑真单.js` — 拿真实单跑回归，第二个参数可指定模型
