# 交接 · 2026-08-18 晚（接上午那份）

上午那份是 `交接-2026-08-18.md`，讲的是白天定的底层逻辑和 17 件改动。
这一份接着讲晚上：**上线 6 条、查明 3 个根子、还欠 5 件**。

---

## 一、现在什么状态

- **线上 = 本地 = `v20260818t`**，18 个文件逐个 md5 核过，后端也一致
- `全部检查.js` **32 套全绿**（今天从 24 套加到 32）
- 项目文件从 121 个清到 71 个，搬走的 50 个在 `桌面\食品系统-旧料\0818清理\`，**一个都没删**

---

## 二、今晚上线的 6 条

| | 改了什么 | 为什么 |
|---|---|---|
| 1 | **老式 `.xls` 能读了** | 微信发来的是真 BIFF 格式，引擎只会解 zip，一句 throw 打发掉。跟 SheetJS 逐格比过：165 个非空格子一个不差 |
| 2 | **人挑的商品不许被推翻** | 8/18 砍折板改过头，连人自己挑的也被闸拦掉 —— 点了没反应 |
| 3 | **存疑那行显示配到哪个货** | 那一列从头到尾没输出过商品名，人只能拿单价倒着猜 |
| 4 | **新建的品/改的价存服务器** | 服务器名单里没这几个字段：词上了云、货没上云 → 死键 |
| 5 | **数量不许被当点位** | 系统拿一整列纯数量问「这是不是点位」，答错记了 12 条还同步上去 |
| 6 | **表样指纹只认结构** | 读法本一直是 0 的根子，见下面 §三 |

另外改了服务器（Caddy）：**页面加了 `Cache-Control: no-cache`**。
原来一条缓存头都没有，浏览器自己决定缓存多久 —— 每次部署完老板可能还在看旧页面。

---

## 三、查明的三个根子（比上面 6 条重要）

### 根子一：「拿不准」在这个系统里不是一个东西

今天补的七处，其实是同一个病 —— **拿不准的时候默认接着往下走**：

| 谁拿不准 | 它干了什么 |
|---|---|
| 匹配闸门 | 把人拍板的当成机器猜的，拦掉 |
| learnWrite | 悄悄不记，一个字不说 |
| 抓取 | 记了帐，没人看，照样交差 |
| AI | 说「拿不准」，代码扔了那列，拿总数顶包 |
| 弹窗 | 拿明摆着的数量去问人是不是点位 |
| 读文件 | 认不出就抛错拒收 |
| 同步 | 一半数据悄悄没上服务器 |

七个地方，七套做法。**每修一处，下次换个地方再犯** —— 这就是老板说的「反反复复打补丁」。

**真正的根治**：单子上每个数都带着「它从哪来」（人填的／表里搬的／算出来的／顶包的），
立一条铁律：**顶包的、来源不明的，不许进单子**。这样七个补丁一个都不需要。

⚠ **但今天量过：1177 个数里只有 20 个（2%）是顶包的，集中在 3 张表**。
为 2% 去动 6900 行页面 + 三个引擎的 37 处，**风险大于收益**。
老板拍板：先不做大的，一条条来。

### 根子二：读法本一直是 0，因为指纹天天在变（已修）

老板问：「我有这么多真表，为什么反复错？读法表为什么是 0？」

读法本按「表长什么样」当钥匙，可那把钥匙**把这一天的内容也算进去了**：

```
轩宝　 5 行 → WWWNWNW【N】
轩宝　18 行 → WWWNWNW【W】     ← 只差最后一列
```

同一家同一张表，**只因为那天备注栏写的是数字不是文字**，就被当成另一张表。
8 家有多张单的客户，5 家全栽在这一项 —— 记住的读法一次都套不上。

已改成只认结构（列数 + 表头词）：**教一次就够 3 家 → 4 家，每张都变 5 家 → 2 家**。
剩 2 家是真换表了（25行5列 vs 2行8列），本来就该重问。

顺手补：**指纹太弱的不给指纹**（没表头且列 < 4）。
实测「2列·无表头」被 5 家客户共用，一家教的会套到另外 4 家头上。

### 根子三：近道绕过了「教一次就记住」

8/16 加的那条「按长相直接抓」的近道，抢在 AI 之前把表读完 →
**那句「这张表我这么读，对吗？」从来没弹出来过** → 老板没机会点头 → 读法本永远空的。

今天加的闸（近道说读不懂就让位）正好把这条路打通了。

---

## 四、还欠着的 5 件

| | 什么 | 状态 |
|---|---|---|
| 1 | **算术规矩**：一列数字加起来 = 总数 → 就是各点位的数量 | 原型跑通（朱鲜生 27 段全对、合计 205 分毫不差），48 张真表量过 **0 张受影响**，**没并进去**。原型在 `试-总数对得上就是数量.js` |
| 2 | **轩宝 4 条死键** | 同一个商品 4 种写法，指向一个已经不存在的品（就地新建的，那时还不上服务器）。要么重建那个品，要么清掉 |
| 3 | **跨单位教不会 + 界面骗人** | 真实数据里 8 条教了查不回来；「又学会 N 个叫法」那句是照行数数的，不管有没有真写进去 |
| 4 | **碧源那两行为什么存疑** | 拿服务器数据怎么跑都是「已学会」，跟老板屏幕对不上。**没查明** |
| 5 | 老式 `.doc` 还是拒收 | 跟今天修的 `.xls` 一模一样的场景 |

另外：**系统识别完不留底**。今天一半的困难来自这个 —— 出了事没有原始结果可复盘，只能从症状倒推。

---

## 五、⚠ 今晚踩的坑

**1. 我改错过一次，测试当场逮住**
让不同客户的切法互相借用 —— 破了老板定的「规矩不许串客户」。
`测试-选完客户重读.js` 三项变红，已撤回。**测试是有用的，别嫌它烦。**

**2. 猜错三次，全是因为手上没真数据**
readOnly、浏览器缓存、断句器 —— 每一次「从症状倒推」都是错的。
真正修对的三次，全是拿到真件之后：诊断页拿到那个 `.xls`、服务器上拿到他存的单、
`桌面\OCR缓存\` 里拿到真表。
**规矩：没拿到真数据不动手。**

**3. 部署会断线**
有一次 scp 传到一半 `Connection reset`（退出码 255）。重传之后**逐个文件 md5 核过**才算数。

**4. `git checkout` 会改换行符**
撤回两个文件之后 md5 跟线上对不上，其实内容一模一样，只是 CRLF/LF。
`diff <(tr -d '\r' A) <(tr -d '\r' B)` 才是真比法。

**5. 中文别拿 `sort()` 比**
JS 按字符码排，「九」排在「八」前面。测试里写成人眼顺序会假报错。

---

## 六、今晚新增的工具

| 文件 | 干什么 |
|---|---|
| `改前改后比一比.js` | **改之前 `拍`，改之后 `比`** —— 盯着 475 个配货结果 + 454 条学过的 + 观麦 9765 行 + 65 种备注写法，变一格就摆出来。上午那种「修坏了别处还不知道」就是靠它兜住的 |
| `诊断-拖文件.html` | 双击就开，把文件拖进去，逐步打出卡在哪一步。老 `.xls` 就是这么 10 秒定位的 |
| `查-多少数是蒙的.js` | 量「单子上的数有多少是顶包的」。当前：1177 个里 20 个 |
| `试-总数对得上就是数量.js` | 上面欠的第 1 件的原型，**没接进开单台** |

---

## 七、CLAUDE.md 要补的

上午那份交接里说的「斤转板作废」还没写进 CLAUDE.md，加上这两条：

> **不许系统折板**：客户写什么单位就按什么单位。仓库有这个品+这个单位就直接用，
> 没有就弹出来让人选。（老板 2026-08-18 推翻 8/2 定稿）

> **人拍板的不许被推翻**：人在下拉里挑的、点「对，记住」的，一律钉住。
> 闸门只管系统自己猜的那一头。

---

## 八、怎么快速验

```
node 全部检查.js                    # 32 套，全绿才许上传
node 改前改后比一比.js 拍            # 动手之前
node 改前改后比一比.js 比            # 动手之后，变一格都会报出来
```

部署：双击 `桌面\部署包\2-上传网页.bat`。
**末尾那句「⚠ 线上版本跟本地对不上」是假警报**（自检 curl 吃了 401）。
要真验：

```
ssh -i ~/.ssh/kaidan -p 50000 root@187.124.137.49 "cd /var/www/kaidan && md5sum *"
```

跟本地 `md5sum` 逐个比。钥匙是 `~/.ssh/kaidan`，端口 **50000**（不是 22）。
