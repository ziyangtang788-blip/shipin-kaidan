# 观麦「订单导入模板」认哪些字段

**2026-08-05 从观麦后台下拉里一个字一个字抄下来的。⛔ 不许再猜，改这份之前先去后台看。**

在哪看：`观麦 → 系统 → 运营设置 → 系统模板 → 订单导入模板 → 某个模板的「详情」→ 对应关系 → 左边「系统名称」下拉`

（那一页有「保存」按钮，模板是**能改的**，不用每次新建。）

## 全部选项

| 系统名称 | 里面该填什么 | 我们有没有 |
|---|---|---|
| 商户名 | 观麦的**商户名**（不是报价单名）| ✅ 有，见 `数据-商户名.js` |
| **商户SID** | **`S3797453` —— 观麦订单页上「江云生鲜配送/S3797453」后面那个** | ✅ 有，就是 `数据-商户名.js` 里每个店的第二个值 |
| 自定义编码 | `JY` 这种，商户的自定义编码 | ⚠ 只有 122/168 个商户有 |
| 商品名 | 实际填的是**客户叫法（规格名）** | ✅ 现在导的就是这个 |
| 商品ID | 待确认：是 D 码（规格ID）还是 C 码（商品编码）| ⚠ 我们导的是 C 码，**没验过** |
| 下单数 | 数量 | ✅ |
| 单位 | 斤 / 板 / 盒 | ✅ |
| 销售单位 | 跟单位重复，映一个就行 | ✅ |
| 单价 | 这家的专属价 | ✅ |
| 下单金额 | 数量 × 单价 | ✅ |
| 客户点位 | `12-11` 这种 | ✅ |
| 商品备注 | 这一行的备注 | ✅ |
| 订单备注 | 整张单的备注 | ✅ |
| 订单分批号 | 观麦的业务字段 | ❌ 不填，走默认 |
| 订单类型 | 同上 | ❌ |
| 税率 | 同上 | ❌ |
| 是否打印 | 同上 | ❌ |

**下拉里没有「商品编码」这一项。**（我之前的文档写错了，已作废。）

## 踩过的坑（别重来）

| 试过什么 | 结果 |
|---|---|
| 只填商户名「江云生鲜配送」（跟观麦一字不差）| ⛔ 商户异常 |
| 商户名 + 自定义编码 `JY`（模板里已正确映上）| ⛔ 商户异常 |
| 往「商户SID」填报价单号 `S2984` | ⛔ 错的 —— S2984 是**报价单**，不是商户 |

**两个 S 码千万别搞混：**

```
S2984      报价单号（salemenu_id）—— 一张价目表，可以被好几个商户共用
S3797453   商户SID（address_id）  —— 这才是「开给谁」
```

## 观麦怎么认一行货

订单详情里那张明细表的列是：**规格ID** `D3196707` │ **规格名** `鲜支竹（2斤）` │ 规格 │ 分类 │ 报价单简称(对外) │ 下单数 │ 单价

所以观麦内部按 **规格ID（D 码）** 认货，显示的「规格名」就是**客户叫法**。
—— 这印证了开单台用客户叫法当商品名是对的，不是 bug。

相关：[[规矩总表 §六]]、`数据-商户名.js`
