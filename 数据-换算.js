/* ⛔⛔ 已停用（2026-08-04）—— 这张表【不再参与算钱】，页面不再调用 convApply。⛔⛔

   老板原话：
     「不要去换算这种有问题的，就找仓库有对应的产品，大部分都是有的。
       如果没有的话，就让他自己单独加一个产品，把这个产品拎出来，
       让自己可以加、可以改。**你换算又换算错了呢。**」

   为什么停：换算系数错一个，金额就差好几倍，而且单据上打出来还是「1 件」，
   肉眼根本看不出 —— 典型的闷声算错，正是 §0.1 最不能犯的那种。
   而「找不到对应产品」最多弹一次窗让人挑或者新建 —— 存疑不算错。

   现在的走法：单位对不上 → 闸门4 去找【同单位】的那条货；
   找不到 → 交给人，用行里那个「＋ 库里没有，新建」自己加一个产品。

   ⚠ 斤转板【不在此列】，那条留着（老板 2026-08-04 选的 A）——
     它是老板 8/2 亲自定稿的规矩、只管某几种产品、凑不整会问人，
     走 GM_MATCH.换算比 那条路，跟这里停用的是两回事。

   ⚠ 下面的数据【留着别删】：这些是工厂明确答复过的事实，
     将来真要给某个客户建「按件卖」的产品时，一件几斤要照这里的数。
     但【绝不许再把它接回算钱那条路】—— 测试-换算.js 里有一条专门钉这个。

   ============================================================
   原说明（存档）：
   单位换算表 —— 下单表上的单位 ≠ 价格库里的计价单位
   ============================================================
   这不是「认错商品」，是「认对了商品但单位对不上」。
   例：万民写「炸腐竹 1件」，价格库里炸腐竹按斤 ¥10.8 卖，一件 10 斤。
       不换算就是 1×10.8 = ¥10.8，实际应该 10×10.8 = ¥108，差 10 倍。

   怎么用：
     解析出一行之后、匹配之前，把 数量 × mul、单位换成 to，
     再拿换算后的单位去匹配。这样匹配那一步完全不用改。

   每条的意思：
     word  下单表上的商品词（用 norm 后的宽松包含匹配，不用写全）
     from  下单表上写的单位
     to    价格库里的计价单位
     mul   数量要乘几（from 一个 = to 几个）
     memo  依据，出错时好回溯

   ⚠ 只写工厂明确答复过的。猜一个换算系数比认错商品还危险 ——
     商品认错顶多差几毛，换算系数错就是差好几倍。 */

(function (root) {
  root.GM_CONV = {

    /* 万民超市小塘店 —— 工厂 8/1 答复：炸腐竹 1件 = 10斤 */
    "S3004": [
      { word: "炸腐竹", from: "件", to: "斤", mul: 10, memo: "表上件¥108，库里斤¥10.8" }
    ],

    /* 万速鲜 —— 工厂 8/1 答复：豆腐串 1斤 = 20串
       （另有「按斤下单每次过称」的规则，那条没法自动，仍然弹窗） */
    "S2987": [
      { word: "豆腐串", from: "斤", to: "串", mul: 20, memo: "表上填1斤，库里按串计价，不换差20倍" }
    ],

    /* 厨鲜达 —— 工厂 8/1 答复：日本豆腐 1件 = 40条
       「按斤下单 = 拆箱称重」那条没法自动，写斤的时候照旧弹窗 */
    "S2983": [
      { word: "日本豆腐", from: "件", to: "条", mul: 40, memo: "工厂：1件=40条" },
      { word: "日本豆腐", from: "箱", to: "条", mul: 40, memo: "有的表写「箱」" }
    ],

    /* 祥熙 —— 同上，1件=40条 */
    "S2933": [
      { word: "日本豆腐", from: "件", to: "条", mul: 40, memo: "工厂：1件=40条" },
      { word: "日本豆腐", from: "箱", to: "条", mul: 40, memo: "" }
    ],

    /* 新又好 —— 工厂 8/1 答复：豆腐串 15串 = 0.75包（库里 20串/包 ¥9.5）
       「炸豆腐 半板 = ?10斤」工厂自己打了问号，没确认，故意不写 */
    "S9086": [
      { word: "豆腐串", from: "串", to: "包", mul: 0.05, memo: "20串/包；15串=0.75包" }
    ],

    /* 鸿森 —— 攸县香干库里按包卖，5斤/包 ¥12 */
    "S2954": [
      { word: "攸县香干", from: "斤", to: "包", mul: 0.2, memo: "5斤/包；30斤=6包" }
    ]

    /* ── 还没敢写的，等工厂给准数 ──
       捷联 S4937  素鸡：工厂说「按条下单每次过称，按实际称重为准」
                   → 没有固定系数，必须人工填，不能自动
       万速鲜      「按斤下每次都要过称」同上
       祥熙        豆腐饼「按称重为准」同上
       新又好      炸豆腐 半板 = ?10斤（工厂自己带问号）
       天晟 S2952  按斤给足斤数（小板5/花板6/中板7/大板15）
                   → 这个不是乘系数，是「给够斤数凑板数」，要单独做
       裕丰 S2947  优质豆腐表上写斤、库里是6斤板货，工厂没说怎么折 */
  };
})(typeof window !== "undefined" ? window : globalThis);
