# 配送开单台 · 后端设计

2026-07-31 · 过渡版本，将来整体并入 ERP

## 要解决的三件事

1. **开完单没记录** —— 开单台不留任何单据列表，打印完就查不到
2. **学到的对照散在各台电脑** —— 换机器要重教
3. **API 密钥在浏览器里** —— 部署到公网等于公开

顺带修一个 bug：单号永远是 `-01`，同一天开第二单就重号。

## 前提（已确认）

- 同时开单 **2-3 人**
- 密钥服务器共用，**不做用户管理**
- **只做开单台**，进销存不动
- 这一版是过渡，**将来整体搬进 ERP**

## 架构

```
浏览器 (https://choeyy88.com)
   │
   ▼
 Caddy ──静态──→ /var/www/kaidan/*.html *.js
   │
   └──/api/*──→ Node (127.0.0.1:8788，外网直接碰不到)
                   │
                   └── /var/www/kaidan-data/
                        ├── orders/YYYY-MM.jsonl   开单记录，逐条追加
                        ├── seq.json               每日单号流水
                        ├── overlay.json           共享对照 + 价格覆盖
                        └── config.json            API 密钥（永不下发）
```

Node 单进程 = 所有请求天然串行，2-3 人并发不需要额外的锁。

## 存储

### 开单记录 orders/YYYY-MM.jsonl

一行一张单，追加写。两个人同时存各写各的行，不可能互相覆盖。

字段名**直接对齐 ERP 表结构**（见 `体系梳理.html`），将来 `COPY` 进 PostgreSQL 即可：

```json
{
  "order_no":"XS20260731-01","order_date":"2026-07-31","deliver_date":"2026-08-01",
  "customer_id":"S2934","customer_name":"...","note":"","source":"开单台",
  "total_qty":502.1,"total_amount":1907.98,
  "created_at":"2026-07-31T10:22:33+08:00","created_by":"laowu",
  "lines":[{"sku":"D3078048","product":"...","alias":"...","raw_text":"...",
            "qty":25,"unit":"板","price":7.5,"amount":187.5,"spot_code":"","note":""}]
}
```

**三条硬规矩**（为将来迁移）：
1. 有编号就存编号（`customer_id` / `sku`），名字只是冗余
2. 一行一条，不嵌套过深
3. 字段名用 ERP 那套

**价格快照**：`price` 存下单当时的价。以后在价格库改价，不影响历史单金额。

### 单号 seq.json

`{"2026-07-31": 3}` —— 服务器发号，`XS20260731-01/-02/-03`。
服务器发号是修 `-01` bug 的正解：三个人同时点保存也不会重号。

### 共享对照 overlay.json

```json
{"version": 42, "updated_at":"...", "data": {"maps":{},"gmap":{},"mem":{},"spots":[],"price":{}}}
```

整体读写，用**版本号乐观锁**：客户端提交时带上拿到的 version，对不上就服务器端合并（按 key 取并集，冲突后写赢），返回新 version。**不会丢任何一方教的东西。**

### 密钥 config.json

`{"anthropic_key":"sk-ant-..."}`。只在服务器进程内读，任何接口都不返回它。

## 接口

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 健康检查，返回版本和数据目录状态 |
| POST | `/api/orders` | 存一张单，服务器发号，返回 `order_no` |
| GET | `/api/orders?from=&to=&q=&limit=` | 查列表（不含 lines，轻量） |
| GET | `/api/orders/:no` | 单张完整内容（含 lines），用于重看/重打印 |
| GET | `/api/overlay` | 拉共享对照 `{version, data}` |
| POST | `/api/overlay` | 存共享对照，带 version 乐观锁 |
| POST | `/api/ocr` | 代理 Claude 识别，密钥在服务器 |

## 前端改动

1. 新增 **「开单记录」** 页：日期/单号/客户/行数/数量/金额 + 搜索 + 点开重看 + 重打印
2. 打印 / 存入进销存时，同时 `POST /api/orders`
3. 启动时 `GET /api/overlay` 拉共享对照；教完 `POST` 推回
4. 识别改走 `/api/ocr`
5. 单号改由服务器发

## 离线兜底

后端连不上时，退回现有 localStorage 行为，页面顶部提示「没连上服务器，记录只存在本机」。**服务器抖一下不能让人开不了单。**

## 错误处理

| 情况 | 表现 |
|---|---|
| 后端挂了 | 前端提示，单照样能打印，记录暂存本地 |
| 磁盘满 | 接口返回明确错误，不静默丢数据 |
| 进程崩溃 | systemd 自动拉起 |
| 密钥没配 | `/api/ocr` 返回明确提示，不是 500 |

## 验证

- **接口**：curl 跑一遍每个端点
- **并发**：同时发 10 个存单请求，验证单号不重、记录不丢
- **前端**：现有 43 项解析测试 + 22 项匹配测试继续跑

## 不做

- 进销存（后期做 ERP 时一起）
- PostgreSQL（数据量太小，文件够用）
- 用户管理（自己人共用）
