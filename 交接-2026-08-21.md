# 交接 · 2026-08-21

前一份：`交接-2026-08-19.md`。那份里 §3.1「合并单/分单」还欠着（8/19 已做导出跟随），
§3.2 那两个原型仍未并，§3.3 的旧欠账今天一条没动。

**线上版本 `v20260821p`**，今天推了 15 次，每次 ssh 上服务器核过。

---

## 一、今天一整天只在打一个靶：朱鲜生那种「一行好几个送货点」的表

老板拖了 9 张真图（在 `C:\Users\李正\Desktop\导入订单`），最后 9 张全读对。
过程绕了很久 —— **原因写在 §四，那是今天最该记住的东西。**

### 1.1 根子：没单位的「30-2」被当成了数量

```
认一格("广州医科大学附属第一医院 (海印院区) 30-2")  → 数量 30、点位「海印院区」
认一格("佛山支队执勤二中队11-8")                   → 一句汉字，没数量  ✓
```

差别只有**点位号前面那个空格**。`extractSegs` 的括号规则「(点位)数量单位」里
**单位是可选的**，所以 `(海印院区) 30-2` 配成了 code=海印院区、qty=30、unit=空。
朱鲜生 8-19 那张四行多开 **¥443**，就是这么来的（8/19 补的是校验网，没治到这儿）。

**判据（死的）**：没有单位的数字、后面紧跟「-数字」= 点位号的前半截，不是数量。
只挡这一种；`(A002)3板`、`(268-本地 干水)1斤`、`(519)12`、`(海印院区)?` 照旧全认。

### 1.2 切不出段 ≠ 这一格没用

上面那一刀挡住之后**点位跟着一起没了**，老板一眼看出来：「你是不是偷懒了？」
他框着原图说的原话：

> 「广州市老人院（上水院区）（老人糊餐）5-5 │ 1板 │ 午餐
>   这一整块全部是点位，这个可以不用去断，它是不是属于这里的断句的问题？」

**他说对了。** 断句器只该在【一格里挤着好几段】时出手；
这张表是【一格一个点位、数量在另一列】，它一出手就必错。

所以：分布列那一格切不出段 → **整格当点位**（号在末尾，前面全是名），
数量照旧从数量列拿。

⚠ 但**一格里有 2 个以上点位号、却一个数量都没有**的，不许拼成一个点位 ——
我真的这么干过一版：`20板 送 5-3 昆仑实业…老人碎菜…`，看着填满了其实六个点全错。
那种要摆成「读不懂」（数量空、留着表上总数、校验网喊出来）。

### 1.3 这种表整个不走 OCR，交给 AI 看图

老板 2026-08-21 那句是转折点：

> **「你是一张表读得全不能代表什么东西，知道吗，不用担心钱，我只担心准确率」**

★ 这句戳中要害：「各段之和 = 总数」**证明不了每个点发多少是对的** ——
三个点位、数量 2/1/1，不管配给谁加起来都是 4，总数永远绿灯，配错了也发现不了。

所以**不拿代码去赌**。判据按【表长什么样】认，不认客户、不认表头，认两种写法：

- **写法一**：同一格里 2 个以上点位号，且不是每段都带着自己的数量
- **写法二**：品名空着的**续行**里带着「点位号 + 数量」

⚠ 一分利「1435:2斤+1440:1斤」那种每段都有量的不碰（OCR 一直抄得好好的）。
⚠ 日期要排掉（`2026-08-01` 长得也像点位号，一分利标题栏踩过）。
⚠ 判据必须排在【认表】和【断句】**前面**，排后面就先被那两条路截走了。

**48 家真表离线扫过：只有朱鲜生命中，零误伤。**

### 1.4 8-20 那种留在代码路（点位后面紧跟自己的数量，配对是明写的）

`引擎-抓取.js` 加了 `OCR表切格子表()`：按「点位号+数量」切段。
采用规矩钉死在引擎里：**每一行都切得出、每行的和都跟总数一分不差才采用；
差一条整张退回老路。** 半张新半张旧比全旧还危险。

---

## 二、回头核对：从「只报不改」改成「真改回来」

老板 8/21 澄清了 8/3 那条：

> 「我说的**不改是不能改品类跟数量**，但是你**点位整个都丢失了**啊」
> 「**如果有那么多弹窗，我就没有意义了做这个事情**」

- 提示词加宽：**数量、点位、漏没漏段**三样都核（原来只核数字和单位）
- 送去核的是**单子上现在这些行**（带行号），**不是 OCR 转写的原文** ★命门
  原文本身没写错、只是少抄了一列，拿它去核 AI 一定答「全对」，白跑
- 点位直接补回；数量填好但挂记号 → 行上一道**青竖条**，照原图对一眼
- 「多出来的行」只标出来，**绝不自动删**
- 没核成（没回话/看不懂）交回 `null`，**不是空数组** —— 空数组会被当成「全对」
- **只弹一个窗**「改过来」，一次点完

实测：真错 3 遍全逮住；**设套 5 遍全答「全对」，一次没瞎改**。

### ⛔ 踩过的坑：这一步原来只长在一条路上

回头核对本来写在「AI 填格子表」那条路的 `.then` 里。
朱鲜生 8-19 走的是「OCR 认字 + 本地断句」那条**省钱路**，读完当场 return ——
**第三步做好了却一次都没跑过**，页面上看着跟没改一样。

现在抽成 `回头核()`，挂在**两个总出口**（`收下文字` / `收下格子表`）上，
断句、明细切段、认表、本子四条路一条都漏不掉，以后再加路也漏不掉。

⚠ 核对用的图必须是**原图 imgs**。OCR 走通那条路上发出去的 content 里
全是文字块、一张图都没有 —— 拿它去核等于让 AI 闭着眼睛看图。

---

## 三、江云那张单：两个毛病

### 3.1 同一段被两条规矩各摆一行（重复）

`(B128 阅景#幼儿-20个)0斤` 同时撞上：

- 8/19「客户写的 0 不许扔」（格子路）→ 留在**原位**，数量框空着等人填
- 8/17「备注写着别的单位就直接加进来」→ 往**队首**另插一行

**老板定：留下面那个（原位那行），顶上那个去掉。**
数量**不许替他填**，他自己手动加；点位里的「-20个」挪走 → 点位就是 `B128 阅景#幼儿`
（不挪的话点位册里会多出一个假点位，按点位对账/减价全对不上号）。

### 3.2 调老单打不开

> 「调不出这张单：Cannot read properties of null (reading 'alias')」

他那张单里几行本来就认不出，**没挑商品就点了确定订单**，存下来 sku 是空的。
`ordReopen` 里 sku 找不到时 idx=-1，可 `how` **一律写死成 frozen** ——
frozen 算「已经定了」，画那一行时直接读 `r.it.alias`，而 `r.it` 是 null → 当场炸。

改：`how: idx>=0?"frozen":"none"` —— 配不上就当【认不出】摆出来让人挑。
⚠ 没在画的那头加判断挡（治标）：how 一路串到存疑拦截、导观麦、rematch。

---

## 四、⛔⛔ 今天最该记住的：我为什么绕了一整天

**每次看到数字错了，我都往下游补一层网，补到第四层才回头问「这个错数是哪一行代码算出来的」。**

四层全是兜底：① 补校验网 ② 明细一格切段 ③ 回头核对填回去 ④ 把回头核挂到总出口。
真正的根子是 §1.1 那一行 —— **老板框着图说「是不是断句的问题」，我才挖到。**

项目规矩里写得很清楚：**「补丁的标志是换张表同样的错再犯一次」**。
8/19 朱鲜生就是同一个错，当时补的是校验网，今天原样又来一遍。**那次就该挖到底。**

**下次先问「这个错数是哪一行代码算出来的」，再问「怎么拦住它」。**

---

## 五、今天踩的坑

**1. ★ 改了引擎却没改缓存戳 —— 白查一整轮**

```
引擎-解析.js    17:51 改过    戳还是 ?v=20260819k
```

页面版本号每次都改、顶上显示新版本，**看着像上线了**；可引擎是**单独的文件**，
靠自己那个 `?v=` 决定要不要重新下载。结果：**无痕窗口里是对的、普通窗口怎么刷都是旧的**。

✅ `全部检查.js` 加了一道闸：**版本号 ↔ 每个 `?v=` 必须一致，落下一个就不给推**。
以后靠机器挡，不靠记性。

**2. 测试守写法不守意图，改一次挂一片**

今天有 6 条旧断言因为「那行字变了」而误报：
`收到([])` 的写法、`innerHTML=头+下一步+补;`、`var 这批图=content.slice()`、
`how:"frozen"`、`只挑【数字和单位】的出入`、`核对结果=[]`。
**都改成守意图了。**（这是 8/19 交接里就记过的教训，今天又撞。）

**3. 两个窗口同时改，版本号撞车**

老板在另一个窗口改了三次（表上一行就是一行、图片路不许并同名、按长相抓那条），
我的 `sed` 找的是旧版本号，没改成。**动手前先 `git log` 看一眼。**

**4. shell 里写正则会被吃掉转义**

`"\s*(\d+)"` 在 heredoc 里变成 `"s*(d+)"`，原型跑出来 0 段，查了半天。
**写正则一律用 Write 工具或 `String.raw`，别在 shell 里拼。**

**5. `python` 是 Windows 商店的占位壳**

`python -c` 静默什么都不做（不报错）。要跑脚本用 `node`。

---

## 六、还欠着的

### 6.1 没挑完商品能不能点「确定订单」—— 老板还没定

现在的规矩：**存疑的行不许导观麦，但存单子不拦**。所以他把没挑商品的行存进去了，
调出来那几行还是空的（现在至少不崩了）。要不要连存也拦住，他没说。

### 6.2 江云那张为什么走了文字路 —— 没查明白

按代码算它该走「认表」（格子路），可实际掉到文字路去了（证据：那行文字带着整句原话，
只有 parseOrder 会留原话）。**中间在哪一步掉下来的没找到。**
不影响正确性，但它本该走更准的那条。

### 6.3 文字路上「别的货」那行的文字是整句原话

糊成一大坨没法看。格子路已经不这样了，文字路没治。
（治法：两条路统一 —— 文字路也照格子路办。老板没拍板。）

### 6.4 更早欠的，今天一条没动

- **合并/分单**：先给哪几个客户开，他还没说
- `试-文字切成表格.js` + 验（51 项全过）—— 他说先别并
- 8/18 夜那批：轩宝死键、跨单位教不会、老式 `.doc` 拒收、学对照记的是脏名字…

---

## 七、今天新增的 3 套回归（已挂进 `全部检查.js`，现在 53 套）

```
测试-明细一格切段.js       25 项   按「点位号+数量」切开；一行对不上整张退回
测试-核对要真改回来.js     53 项   点位补回、数量填好框起来；只弹一个窗；没核成≠全对
测试-一行多点位走AI.js     19 项   两种写法都认；48 家真表零误伤
```

另外补进老测试的：
`测试-点位号不许当数量.js` +19 项（第六、七、八节 —— 今天的真根子都在这儿）、
`测试-零数量要摆出来.js` +8 项、`测试-订单记录.js` +3 项。

**三层验证网每次都跑，一次没跳过**：`全部检查.js`、`真表快照.js 比`（35 张真表）、
`改前改后比一比.js 比`。今天 35 张真表**一格都没变过**。

---

## 八、调试留下的东西（别删）

- `调试-格子表-*.json`（35 个）—— **真表快照的底料，删了快照就废了**
- `调试-OCR原始/`（48 家）+ `调试-导入订单OCR/`（9 张）—— 真表的 OCR 结果，
  再验这条路不用重新花钱
- `调试-朱先-*.json` / `调试-朱先2-OCR.json` —— 今天查根子的证据
