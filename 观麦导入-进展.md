# 观麦批量导入 —— 交接文档

**2026-08-06 更新。这份可以直接发给接手的人，看完就能跟上。
所有结论都是在观麦后台一次次实测出来的，不是推的、不是猜的。**

---

## 一、这件事在干嘛

我们自己的「配送开单台」把客户发来的订单认成一张销售单。
认完之后，这张单要进**观麦**（公司在用的进销存系统，`station.guanmai.cn`）。

以前是人工在观麦里一行行敲。现在改成：**开单台导出一个 xlsx → 观麦「批量导入订单」上传 → 一次进去。**

老板定的顺序是：**先导成功，再优化。**

---

## 二、现在的状态：✅ 通了

真单实测：**48 种商品、807.35 元，一次导入，零报错、零黄条、点位备注全带上。**

金额跟上一版靠名字导的那张**分毫不差**，说明换成按码取货没有认偏任何一条。

---

## 三、定稿的格式：10 列，顺序不能动

```
1 商户名   2 商品名   3 下单数   4 单位     5 单价
6 商品备注 7 订单备注 8 客户点位 9 商户SID  10 商品ID
```

**观麦按【第几列】认，不按列名。** 模板里映了几列，文件就得正好是那几列、那个顺序。
多一列少一列都会把后面全挤歪，报「第N列应该为：xx」。

要加列**只能往末尾加**，而且必须去后台把模板一起改，两边同时动。

### 观麦后台模板怎么设

`观麦 → 系统 → 运营设置 → 系统模板 → 订单导入模板 → 新建/详情`

左边「系统名称」和右边「导入表格内名称」两边选一样的词，10 行，顺序照上面。

> ⚠ **右边那列列的是你上传的那个文件的表头**，不是观麦的字段。
> 所以要先在「导入表格」那里把文件传上去，右边下拉才有东西可选。

### 导入时

- **运营时间必须选「默认运营时间」** —— 不选连报「商户异常」，查了半天才发现是它
- 预设模板选那个 10 列的
- 标题所在行 = 1

---

## 四、三个关键结论（这轮实测出来的）

### 1. 商户SID 说了算，不认名字

`S3797453` 这种，是**商户**（开给谁）的唯一码，一家门店一个。

**验法**：故意把商户名写错成「江云」（观麦里叫「江云生鲜配送」），SID 填对。
**结果**：6 行照样落到 `江云生鲜配送/S3797453`，金额全对。

**意义**：我们价格库 177 家客户里，有 62 家的「报价单名」跟观麦的「商户名」对不上
（9765 行真单里 5742 行两者不同）。以前光靠名字导，这些全进不去。有了 SID 就不看名字了。

> ⚠ **两个 S 码千万别搞混：**
> ```
> S2984      报价单号（salemenu_id）—— 一张价目表，好几个商户可以共用
> S3797453   商户SID（address_id）  —— 这才是「开给谁」
> ```
> 踩过的坑：往「商户SID」填了报价单号，一路报错。

### 2. 商品ID 填 D 码，能根治「商品未完全识别」

`D3196709` 这种，是观麦内部的**规格ID**，一张报价单里一个货一个码。

**病根**：观麦本来是拿**商品名**去这家的报价单里搜。
我们价格库 5680 条里有 **302 条名字是同一家另一条的子串** ——
「香干」包在「攸县香干 / 卤香干 / 白香干 / 五香干（茶干）」里面，
观麦一搜搜出一堆，拿不准，就弹黄杠「商品未完全识别，为你推荐…」要人一条条点。
**涉及 96 家客户，一半以上。**

**验法**：拿原来弹黄杠那两行「香干」，第 10 列填 `D3196709` 导一次。
**结果**：黄杠没了，货是对的那条，单价 3，13.50 对得上。

**码从哪来**：本来就是从观麦自己的报价单里导出来的，同源，5680 条**一条不缺**。
代码里取 `sku` 字段（价格库第 7 个），**不是 `spuId`（C 码，商品本体，跨客户通用）**。

### 3. 多门店客户必须选店

观麦的结构是这样的：

```
报价单 S2984「三颗菜」   ← 价格在这里，一份，所有店共用
   ├── 商户 S3797469  三颗菜三水仓    ← 开单开给这个
   ├── 商户 S38xxxxx  三颗菜白云仓
   └── 商户 S39xxxxx  菜运达供应链
```

一个客户（总公司）有好几个店/仓，价格调的都是总公司那一张报价单，
但开单要开到具体的店。**光给「三颗菜」这个名字，观麦不知道开给哪个仓。**

数据我们有，`数据-商户名.js`：**124 张报价单 / 168 个店，SID 一个不缺（0 缺失）**。
其中 **106 家只有一个店**（自动带出，人不用管），**18 家有分店**（必须人选）。

有些分店名字八竿子打不着，只有码能认：
```
S13280 → 菜云(佛山)食品科技  S3898600
         菜仟箩              S3923741     ← 同一张报价单
```

---

## 五、代码改了什么（⚠ 本地已改，**还没上传**）

跑过 `node 全部检查.js`，**全部通过**。但**没有**双击 `桌面\部署包\2-上传网页.bat`，线上还是旧版。

### `导出-观麦.js`

| 改动 | 说明 |
|---|---|
| 列表从 14 列砍成 10 列 | 删掉 商户编码/商品编码/商品ID(旧)/销售单位/下单金额 —— 就是它们把商品名顶到第 3 位，报「第3列应该为：商品名」 |
| 顺序改成跟观麦模板同序 | 见上面第三节 |
| **修了一个会开错客户的 bug** | 「商户SID」这一列原来填的是 `o.customer_id`，那是**报价单号**（好几个店共用）。改成取门店自己的码 |
| 商户名和 SID 走同一个 `门店()` 函数 | 各算各的迟早出现「名字是A仓、SID是B仓」——观麦按 SID 收货，货就送错店了，页面上还看不出来 |
| 加第 10 列「商品ID」填 `sku`（D 码） | 见上面第四节 |

真数据跑出来长这样：
```
江云生鲜配送 │ 香干 │ 3 │ 斤 │ 3 │ │ 整单备注… │ A014 东富#餐 │ S3797453 │ D3196709
```

### `配送开单台.html`

有分店的客户，**不选店不给导**。原来只在点「导观麦」那一刻才弹窗拦，
人可能一整张单开完了才被拦回来。现在改成：

- 加了 `观麦店数(cid)` / `要分店(cid)` 两个函数（只数观麦的店，人自己加的店不算）
- 门店下拉第一项，有分店时从「（不分店）」改成「**⚠ 还没选门店**」——原来那是个错的默认值
- 新增 `催选门店()`：没选就把下拉框描红 + 底下红字「这家在观麦有 N 个门店，得选一个 —— 不选不给导」，选上了自动消失
- 原来的硬拦截保留不动

**没动任何 id、没动页面结构、没动匹配逻辑。**

### `测试-导观麦.js`

断言跟着改了：10 列顺序钉死、模板没映的列一律不许出、商户SID 不能是报价单号、
三颗菜三个门店逐个比对「名字和 SID 是同一家」、商品ID 必须是 D 码。

### 新增 `观麦-爬报价单价格.js`、`工具-价格对账.js`

见下一节。

---

## 六、价格库重爬 —— ⚠ 进行到一半，卡在一个 bug

### 为什么要重爬

`数据-价格库.js` 是 **2026-07-28** 的快照。观麦那边之后改过价、加过商品、开过新客户。
不重爬就一直用旧价 —— **导进观麦的金额直接是错的**。

### 已经做了什么

写了 `观麦-爬报价单价格.js`（浏览器 F12 粘贴运行），用的两个接口：
```
/salemenu/sale/list?type=-1&with_sku_num=1&q=              报价单清单
/product/sku_salemenu/list?salemenu_id=Sxxxx&offset=&limit= 每张的商品价格
```
已经爬过一次：**185 张报价单、5737 条商品、6 张空的、零失败**（旧库是 183 张 / 5680 条）。

又写了 `工具-价格对账.js`：拿新 CSV 跟**现在库里那份**逐格比，只输出变了的那几条。

### 对账当场抓出一个大 bug

```
华丰超市大沥店  日本豆腐（件）   20 → 2000
华丰超市大沥店  卤水豆腐         18 → 1800
```

5052 条"改价"全是整整 **100 倍** —— **观麦接口里的价格单位是「分」，不是「元」**，
爬取脚本直接当元写进 CSV 了。

**要是跳过对账直接换库**：生成成功、`全部检查` 全过、页面照常打开，
然后每一张单的金额都是 100 倍。这种错没有任何自动检查拦得住。

### ⬜ 下一步（等老板点头）

1. 改 `观麦-爬报价单价格.js`：`销售价(元)`、`销售单价(元/基本单位)`、`库存均价(元)`、`最近采购价(元)` 四列 ÷ 100
2. 重爬一次（几分钟）
3. 再跑 `node 工具-价格对账.js` —— 改价条数应该从 5052 掉到几十条，那几十条才是**真的**改过的价
4. 一条条看完没问题，再右键跑 `工具-生成价格库.ps1` 生成新的 `数据-价格库.js`
5. `node 全部检查.js`
6. 上传

**不要**直接拿现在这份 CSV 除以 100 —— 脚本里的错没改，下次重爬又是 100 倍。

### 顺便比出来的（等价格理顺再看）

| | 条数 | 为什么要留意 |
|---|---|---|
| 改叫法 | 18 | 客户叫法变了，以前学过的对照可能对不上 |
| **改单位** | **5** | **单位变了数量会跟着错，比改价还危险** |
| 新增 / 没了 / 新客户 | 62 / 5 / 2 家 | 正常增减 |

---

## 七、还欠着什么

| # | 欠什么 | 挡不挡上线 |
|---|---|---|
| 1 | **价格库重爬没做完**（上面第六节） | ⛔ 挡。价不对就是错钱 |
| 2 | **18 家有分店的：每个店各开一张单，还是总公司开一张？** 要问工厂 | 不挡，但不定就不知道该拆几张单 |
| 3 | **53 家客户查不到 SID** —— 观麦近期没它们的订单，现在这份是从订单反推的。要爬**商户列表**才能补齐（接口地址还没拿到，得去观麦后台 F12 抓一次） | 不挡。这 53 家现在 SID 留空，退回靠名字 |
| 4 | 「订单备注」进没进观麦，还没在页面上确认过（老板说先不管） | 不挡 |

---

## 八、这轮踩过的坑（别重来）

| 坑 | 真相 |
|---|---|
| 报「第3列应该为：商品名」 | 不是模板错，是文件有 14 列去撞 10 列模板，多的列把后面挤歪了 |
| 模板漏映了「客户点位」 | **不报错、不吭声**，导进去点位就是空的。加列要插在对的位置，不能都往末尾堆 |
| 填「自定义编码」`JY` | 那是**商品**的自定义编码，不是商户的 —— 6 行全报「商品不存在」 |
| 「商户SID」填报价单号 `S2984` | 错的，那是价目表不是商户 |
| 只填商户名，报「商户异常」 | 真凶是**运营时间没选默认运营时间** |
| 爬下来的 CSV 叫 `xxx (1).csv` | 「下载」文件夹里有同名旧文件，浏览器自动加的 (1)。生成脚本只认不带 (1) 的名字，不改名就等于拿旧数据重生成一遍，**而且你看不出来** |
| 价格 100 倍 | 观麦接口的价格单位是**分** |

---

## 九、死规矩

- **不许猜。** 这条是拿三次白跑换来的：猜了三次字段名，三次全错。要什么信息就直接开口让人去后台看。
- **动开单台前必须先问。** 在跑的东西不许直接改。
- **认错必须为 0。** 存疑不用管，但读错一个数字就是事故。
- **换价格库之前必须先对账。** 价直接是钱，人工录的价会错，自动检查拦不住。
- **上传前必须跑 `node 全部检查.js`。**

---

## 十、相关文件

| 文件 | 是什么 |
|---|---|
| `CLAUDE.md` | 工作规矩（每次会话必读，很短） |
| `说明-规矩总表.md` | **唯一权威**，523 行，按节号查 |
| `观麦-导入字段清单.md` | 观麦导入模板的全部字段，从后台一个字一个字抄的 |
| `数据-商户名.js` | 124 张报价单 / 168 个店 / SID |
| `数据-价格库.js` | 5680 条（**7/28 的旧快照，待更新**） |
| `导出-观麦.js` | 生成 10 列 xlsx |
| `观麦-爬报价单价格.js` | 重爬价格（**待改 ÷100**） |
| `工具-价格对账.js` | 换库前对差异 |
| `工具-生成价格库.ps1` | CSV → `数据-价格库.js` |
| `全部检查.js` | 一次跑完所有回归 |
| `记忆/MEMORY.md` | 老板拍板的原话和为什么 |

三者对不上，**以老板最近那句为准**。

---

## 附：另一个项目（外贸获客系统）

同一天顺手处理的，跟配送系统没关系，写在这里免得遗漏。

桌面上的 `my-claude\au-lead-gen` 文件夹**曾被误删**（全盘搜过，回收站也没有）。
已从 GitHub `sherry313/leadgen` 拉回，**61 个文件完整**，最后一次提交 07-28 21:47。
查过：那次 push 之后一个项目文件都没再改过，所以**没有丢东西**。

还缺 `.env`（22 个环境变量：Supabase / Instantly / Firecrawl / Apify / Anthropic / Gmail / Google Sheets）——
按规矩不进 Git。要跑起来的话从 VPS `72.60.111.20` 拷回来最快。

桌面已建 `外贸系统-开工.lnk`（VS Code 打开）。
