# 开单台 · 规矩总表

> 这一份是**唯一的权威**。代码注释、测试、我的记忆都可能过时，以这份为准。
> 每条后面标着「哪张真单逼出来的」和「哪个测试守着」——改之前先看那个测试。
>
> 最后更新：2026-08-03

---

## 零、最高原则

### 0.1　认错必须是 0

老板原话：「你可以让他去选择，让他去学习，但是不能识别错误。」

- **存疑不算错**——弹窗让人点一下，可以接受，不许为了少弹窗去猜
- **闷声算错才是错**——不弹窗、直接落到单上、金额还错了，这叫事故
- 硬门槛：`node 全部检查.js` 里的 `体检-会不会认错.js` 必须是 0，否则不许上传

### 0.1b　「没写数量」分两种，处理完全相反 ★（2026-08-04）

老板：「**没写数量的全部当 0 不进入系统，因为客户不想改他的订货模板。**」

| 情况 | 怎么办 | 为什么 |
|---|---|---|
| 图上**明明有数字**，我们没读出来 → 标 `?` | **留住 + 硬拦**，必须人填 | 我们的错，放过去就是漏货 |
| 客户**压根没写数量**（只有商品+单位） | **整行丢掉，不进系统，不报警** | 客户这次不要这样货 |
| 分布里**有条目**却一段都读不出 | **留住 + 报「被吞了」** | 那是我们没读懂，不是客户没写 |

**实例**：花鹿碧翠那张微信群聊截图，23 行里 9 行只有单位没有数字
（`三角豆泡斤`、`米豆腐盒`、`鸭血件`…）。以前全弹成「看不清」还拦住确定订单 —— 误报。

⚠ **第三行是命门**：2026-08-04 单字键事故就是「AI 填了 7 段、我们一段都没认出来」。
要是把它当成「客户没写」丢掉，整张单会静默变空。**判据只看 `分布` 数组里有没有条目**，
不是看有没有读出数字。守着的：`测试-读结构.js` §十二（含一条专门钉这个的 ★）。

**另有一条豁免（老板 8/3 明确同意，别当 bug 修）**：
下单表上有、但格子表里没座位的字段（开单员/验收人/税率/毛重/配送车次）
—— **直接丢掉，什么都不说**。理由：单据是给客户对钱对账的，对方的经手人跟钱无关。

---

### 0.2　遇到反常，拎出来问，别改规则 ★

老板 2026-08-03 原话：
> 「像这种情况，你不用把它整个规则改掉。
> 　以后他在一个东西里面的单位突然改变，你只需要把这单独的这一个东西拎出来问就可以了。
> 　我不需要你把什么都按照规则来，我只需要你把有问题的弹出来。」

**改规则的代价永远比弹一次窗大。** 一条规则要管几百家客户、上万行单；
为了一个特例改规则，等于拿全部去赌一个。

判断口径：

| 情况 | 怎么办 |
|---|---|
| 全公司通用的生意规矩（烟干不写就是薄、斤转板整除优先） | 写成规则 |
| **某一行反常**（单位突然变了、备注写着别的货、数量对不上总数） | **拎出来问，不改规则** |
| 拿不准是哪种 | 先拎出来问，攒够三五次再看要不要成规则 |

**反面教材（2026-08-03 当天来回改了两次）：**
江云那张 `(B180 光华#幼儿-6盒)0斤` 写在嫩豆腐行里。
我先加了道「盒跟斤对不上就不算」的闸门 → 又撤掉 → 又加回来。
其实一开始就该**直接把这一条拎出来问**——单位突然变了，机器本来就不该替人拿主意。

现在的做法就是这个：不算进这一行，**单独摆出来**「这里写着 6盒，跟这一行(斤)对不上，多半是别的商品」。

---

## 一、认识一张单（解析）

### 1.1 数量从哪来

| 情况 | 怎么办 | 逼出来的单 |
|---|---|---|
| 一串加法 `1+3+2+4+2+1` | **全加起来** = 13 | 创鲜汇 8/2，11 行错了 8 行，少收 ¥817 |
| 备注里写了数量 | **以备注为准**，数量列不算 | 江云 8/2，数量列 0、备注「4块」 |
| 备注里夹着别的字（`靓，大，要空心那种`、`2楼`、`20号送`） | **一律不动** | —— |
| 只写了名字、没写数量 | **整行不进单**，但列出来给人看 | 创鲜汇的「常订品名单」10 项 |
| 明明白白写 0 | **不进单**（今天不要） | —— |

**改过就要摆出来**：备注顶掉数量列的行，标「数量取自备注「X」，表上写的是 N」。

守着的：`测试-解析引擎.js` N/O 两节

### 1.2 一行里好几个数字，哪个是数量

**先找「斤/板/件/包」那一格，紧挨着它的数字才是数量。**

找不到单位格才退回「取最后一个数字」。

> 观麦导出表的「单位编号」列排在数量右边，取最后一个就全取成点位号了——
> 三颗菜 8/2 那张 32 行全错，大豆泡 6斤 出成 218 斤。

守着的：`测试-解析引擎.js` P 节

### 1.3 表格的列不能串

- 空格子必须占位（`split(/\t/)` 不是 `\t+`），塌一格后面全错位
- 「预定数」「订货数」「采购数」都算数量列
- 「单位编号」「企业编号」「客户简称」算**点位**，不是数量
- 「规格」列并进商品名（分得开 7斤板 ¥8 和 15斤板 ¥17）
- 点位跟它的数量永远在同一格，中间不许隔 Tab

守着的：`测试-解析引擎.js` K 节

### 1.4 点位的四种写法

```
(020#西餐厅)6斤+(089)3斤          雅食乐 / 碧源
2盒*KC概念餐厅(月付)【519】        御口福
广州交通管理局:7板+京都电工:3板    惠丰（观麦导出的「明细」列，必须有 Tab）
桃盛酒店，山水豆腐 3 盒            逗号前面是地方（酒店/饭堂/税局/派出所…）
```

**逗号那条只在前半段「像个地方」时才切**——「客家豆腐，老豆腐 3斤」一个字都不许切。

**一段一行，一段都不并 ★**

老板 2026-08-03：「它同一行里有多少你就出多少行，**不要做多余的动作**。
至于合并，那是以后我另外要加这个功能。你只管把它识别对、跟做对行数就行了。」

原来 `compute()` 按「点位＋减价额＋备注」分组，同一个点位号出现两次就并成一行。
江云 8/3 踩到：识别读出

```
(A002 鹤峰#幼儿餐)3板 + (A002 鹤峰#幼儿餐)2板
```

第二个号其实是 **A056**，被看成了 A002。一合并 → 一行 5板，点位栏印成
`A002 鹤峰#幼儿餐 A002 鹤峰#幼儿餐`，**「号看错了」这件事就被盖住了，反而查不出来**。
不并，两行摆着，重号一眼就看见。

- 顺序照单据原样（`segs` 本来就按出现顺序），别拿 `Object.keys` 兜一圈 ——
  点位是纯数字（【519】）时 JS 会当整数键重排，行序就跟客户写的对不上
- 合并是**以后**单独加的功能，现在一个字都不写

### 1.5 商品名里的字，什么该留

| 该留 | 该去 |
|---|---|
| （大）（小）（厚）（薄）（老）（嫩） | 点位号 |
| 规格：（5斤装）（净重15斤/板）（含水15%） | 页码、签名栏、合计行 |
| 括号里的真商品名：豆腐嫂**（大板豆腐）** | |

⚠ **（大）（小）丢了是最要命的错**——两行变成一模一样，价钱差一倍，而且后面没法分开改。
识别提示词里有一条最高优先级规则专门管这个。

---

## 二、配到哪个货（匹配）

一行字进来，**按这个顺序**过 7 道。改顺序前先想清楚。

```
1. matchCore        先按名字找候选（学过的 > 预置 > 精确 > 光名字 > 叫法本 > 包含）
2. 默认那头          烟干/油片不写厚薄 = 薄（老板定的生意规矩，直接拍板）
3. 词冲突            表上写「大」配到「小」→ 拦，把写对那边排第一
4. 单位对不上         写「板」配到按斤卖的 → 改配同单位的；规格还对不上就问人
5. 同单位的排前面      找不到同族的，至少把单位对得上的排前面当默认
6. 斤怎么算 ★         按斤还是按板（见第三节，最要紧的一条）
7. 认一遍            并了规格反而认不出的，拿客户原话再试一次
```

**为什么「斤怎么算」放第 6**：前面几道定「是哪个货」，这一道只管「按斤还是按板」。
放前面的话，货还没定就去算斤板，算了也是白算。

**为什么「认一遍」放最后**：它要看前面所有闸门跑完敢不敢拍板，不敢才用原话再试。

**代码里长什么样**（2026-08-04 补，免得再记错）：
`各道闸门()` 装的是 1~3；`认一遍()` 装的是 各道闸门 → 4 → 5 → **6 斤怎么算（它是最后一道）**；
`matchOne()` 调 `认一遍()`，不敢拍板就拿原话**再调一次** —— 那次就是闸门 7。

⛔ **单位换算已停用（2026-08-04）**。原来闸门 4 之前还有一道「硬编码单位换算」
（件→斤 ×10、斤→串 ×20，在 `数据-换算.js` 里）。老板：
> 「不要去换算这种有问题的，就找仓库有对应的产品，大部分都是有的。
> 　如果没有的话，就让他自己单独加一个产品……**你换算又换算错了呢。**」

换算系数错一个金额就差好几倍，单据上还打「1 件」，肉眼看不出 —— 闷声算错。
现在：**单位对不上 → 闸门4 找同单位的货；找不到 → 交给人「＋ 库里没有，新建」。**
表里的数据留着没删（工厂答复过的事实，将来建产品要用），但**绝不许接回算钱那条路**。

⚠ **斤转板不在此列**（老板选的 A）——它是老板 8/2 定稿的规矩、只管某几种产品、
凑不整会问人，走 `GM_MATCH.换算比`，跟停用的那套是两回事。

守着的：`测试-匹配.js`（32 项，含 **7 项专门钉闸门顺序**）、`测试-按单价定夺.js`、
`测试-斤转板.js`（49 项）、`测试-换算.js`（21 项，含 4 项钉「不许把换算接回去」）

### 2.1 什么时候敢自己拍板

**只有这几种**（`引擎-匹配.js` 的 `ASK`，全系统唯一一份）：

| 敢拍板 | 要问人 |
|---|---|
| learned 学过的 | fuzzy 光名字沾边 |
| seed 预置的 | cross 别家这么叫 |
| exact 精确命中 | gmap 叫法本 |
| default 厚薄规矩 | none 认不出 |
| byweight / fitboard / byunit 斤板规矩 | nocust 客户没定 |

这份名单只能有一处——页面拿它决定弹不弹窗，体检拿它决定算不算事故。
两边各写一份的话，总有一天页面放行了、体检还当它会问人。

### 2.2 单价能反推

客户单据上自己写了单价（`阳山豆腐卜 1斤 ¥8`），拿来定夺：

- **只在本来就要弹窗时用**，不抢已经确定的答案
- **必须只有一条候选对得上价**才算数，两条都是 ¥8 就还是问人
- 一条都对不上（价格库过期）→ 当没写

守着的：`测试-按单价定夺.js`

---

## 三、按斤卖还是按板卖 ★

**老板 2026-08-02 当面确认两次，跟观麦历史单据冲突时以这条为准。**

原话：
> 「客户如果给的是斤，那么你要对应他这个产品里面有没有可以整除的斤的数量。
> 　如果没有，就是按单斤卖；如果有，就是按板卖。如果他给的是板，你就直接按板来。」
> 「仓库里面系统产品有可以整除的，要优先整除。」
> 「按斤的规则来，不按观麦的规则来，按我的规则走。」

```
客户写「板/件/盒」  →  直接按那个单位算，不折不换

客户写「斤」        →  配到的本来就是论斤卖的？ → 一步都不许动
                    →  是板货？拿这个货所有板型去除斤数
                         · 除得尽 → 按板卖（整除优先）
                         · 除不尽 → 改按单斤卖
                         · 好几种都除得尽 → 问人
                         · 除不尽、又没有论斤的 → 问人
```

**实例**（裕丰：5斤板¥5.5 / 6斤板¥6.5）：

| 报数 | 结果 |
|---|---|
| 18斤 | 18÷6=3 → **6斤板 3板** |
| 15斤 | 6除不尽、5刚好 → **5斤板 3板** |
| 30斤 | 6÷刚好 → **6斤板 5板** |
| 17斤 | 都除不尽 → **按斤价**（存疑）|

**⚠ 最容易闯的祸**：客户词里写着「斤」「散装」的，去凑板就是自作主张。
`散装攸县香干 45斤` ÷5=9 刚好整除，折成 9 包就错了——观麦开的是 45 斤散装。
不加这道闸，9758 行真单里凭空多出 60 多条认错。

### 3.1 一板几斤，从哪读

三处，越靠前越可信：
1. 规格字段：`12盒/件`、`40条/件`、`0.4斤/块`
2. 商品名里带的：`水豆腐7斤/板`
3. 商品名括号 + 计价单位：`水豆腐（6斤）` + 单位板 → 一板 6 斤

**范围一律不算**：`（14-15斤）` 算 14 还是 15 差一板的钱，宁可问人。

### 3.2 已知冲突点（别照观麦改回去）

碧源 S2949「水豆腐(5斤装)」1斤+2斤：
- 按这条规矩：除不尽 5 → 按斤 **¥3.90**
- 观麦那张单：折成 0.6 板 **¥3.18**

差 ¥0.72，**以这条规矩为准**。

守着的：`测试-斤转板.js`（49 项）、`体检-斤还是板.js`（9758 行真单）

---

## 四、学习

### 4.1 什么时候学

- **点「确定订单」= 学一遍**——这一单每行的「客户写的词 → 定下来的货」全记住
- 在下拉里挑一个 = 学
- 点「✓ 对，记住」= 学

老板原话：「这个确定就是学习的过程，我得把它学好。」

### 4.2 同一个词好几行

**默认统一改。** 挑完弹一句：

> 这张单里「水豆腐/板」一共有 23 行。
> 　确定 = 全部改成「尝元小板豆腐」，以后也记住
> 　取消 = 只改这一行（这个词就不记成对照了）

老板原话：「这种改我也没法统一改，我得一个个改。这种就是要统一改的。」

**选「只改这一行」的意思是：这个词真有歧义**（客户漏写了（大）（小）），
那就不该记成对照——记了下次还是错。

### 4.3 教错了能改回来

「忘掉重学」→ 本地删 + 记一笔 `gone` 一起送服务器，服务器照着删。
重教之后那一笔自动撤销。

守着的：`测试-忘掉重学.js`（28 项）、`测试-确定就学会.js`（17 项）

### 4.4 存在哪

**服务器。** 换电脑、换浏览器、清缓存都不丢。

- 开页面先跟服务器对一次
- 学到新东西 1.5 秒后自动推
- 连不上就先存本机，下次连上自动补推
- 「上次同步后本机动过哪些键」记在 `dirty` 里，同步时盖回去

守着的：`测试-学习存服务器.js`（15 项）

---

## 五、开单

### 5.1 单据上印客户的名字

老板原话：「我开出的这张单的名字要按照客户给我的名字去开。但是你实际对照的这个东西，
它要是我们仓库有的东西就可以了。」

→ 销售单印**客户写的词**，匹配只是为了找到**我们的货和价**。

### 5.2 加单

- **调出来改** → 老行**冻住原样不动**，不重新识别
- 放新图/新文件进去 → **只解析新来的那段**，接在后面
- 还是**同一张单号**，重存是覆盖
- 重复商品**另起一行**，不合并相加

老板原话：「我调出来之后，我这里面东西我是不改的，我另外再放一张图进去。」

⚠「识别并计价」是**整张重算**的意思，会解冻老行——点它会先问一句。

### 5.3 押金

按**普通商品**收钱（中板押金 ¥5、小板押金 ¥5），下面的结算是另一码事。
数量不自动塞——摆出来 + 一键加 + 数量随便改。

### 5.4 同一张图放两次

**直接报重复，不加进去**。只有不一样的图才合并。

### 5.5 数字看不清 ★

老板 2026-08-03：「**不要逼他写个数字，不确定的就写 ?，我们自己输入。**
总数反推也要做，双重检验。有问题的那几行变个色，改好之后变回原来色，同时报错消失。」

**为什么**：识别错的数字（4 看成 5、6 看成 8）规则永远挡不住 —— 字就长得像。
猜出来的数看着跟真的一样，没人查得出来；写 `?` 摆在明面上，扫一眼就补掉。
**写 ? 不算错，猜一个数才是错。**

两道检验，各管各的：

| | 抓什么 | 颜色 | 数量框 |
|---|---|---|---|
| **没看清** | 识别自己说「这个字我读不出」 | 🟠 橙 | 空着，提示「填数量」 |
| **总数对不上** | 各段之和 ≠ 客户填的总数量列 | 🔴 红 | 照填，只提醒 |

- 提示词里明写：数量位看不清就写 `?`，**只有数量那一位**可以写（名字、点位不许，读不出就整行不写）
- `?` 进来的段：`没看清:true`、`qty=0`，但**不许被「数量 0 就丢掉」那条规则丢掉**（它不是 0，是不知道）
- 人一填数量，`没看清` 立刻抹掉 → 颜色回原样、上面的提醒条自己少一条
- 提醒条数是从当前表格现算的，不是识别时拍的快照
- **空着不许确定订单、不许导观麦**，跟「一行存疑都不许导」一条线

守着的：`测试-订单记录.js`（28 项）、`测试-页面能跑起来.js`（121 项）、`测试-解析引擎.js`（165 项）

---

## 五点五、怎么读这张表 ★（2026-08-03/04 大改）

这一整节是**读表那一层**的规矩。二、三层（配价、斤转板、开单）一个字没动。

### 5.5.0 总则：列的含义不许写死 ★（2026-08-09）

老板 2026-08-09：

> 「不是让你去纠正每一个客户，我要你做的是**把这个功能加进去**。
> 　每个客户的每个表格，他们**只需要第一次做的时候告诉你**就可以了，
> 　不需要你去把每个客户具体化。」

> 「我让你做的就是**一个总规则**，先读懂这个表，哪个地方属于什么东西。」

**先读懂表，再搬数据。两步，不许合并。**

**第一步 · 读懂**——这张表每一块是什么？

自己认得出的，自己认，不问：

| 长什么样 | 判定 |
|---|---|
| 中文词 ＋ 在这个客户的报价单里查得到 | 商品 |
| 斤 / 件 / 板 / 盒 / 公斤 / 袋 | 单位 |
| 带小数的钱 ＋ 跟这个客户的价对得上 | 单价 |

**认不出的，停下来问人。** 典型就是一串光秃秃的数字（`1440`、`1461`）——
点位号、数量、序号长得一模一样，**没有任何长相上的特征能分开**。

**第二步 · 搬**——读法定死、人点过头了，才照着搬。这步纯本地，数字不过模型。

#### 写死的只有这一条 ★（2026-08-09 下午加）

> 老板 2026-08-09：「**他的分店一次只可能是打一张单**」

所以：**表里横着一排收货方，必然是【点位】，不可能是分店。** 这条**准写死**，不用问。

—— 一排出现 41 个，只可能是一个客户底下的 41 个收货点，
不可能是 41 家分店各下一单。

**但还有三种认错的可能，各有一道网，长得像就拦下来问：**

| 一排里长得像 | 认错了会怎样 |
|---|---|
| **日期**（`8/1` `周一` `2026-08-01`） | 按天排的订货表 → 当成 5 个点各 10 斤 = **多发 4 倍** |
| **规格**（`大板` `中板` `5斤/板`） | 同一个品的 3 种规格 → 当成 3 个点，**价按一个算，钱全错** |
| **餐次**（`早餐` `午餐` `晚餐`） | 数量对，但点位栏印「早餐」，**送货的不知道送哪** |

⛔ **表头写的字不作数。** `4号豆制品.xlsx` 那张表自己写着「门店名称」，
底下是 万寿 / 应元 / 员村 —— **那是点位，不是门店。**
客户的表是他们自己系统导的，用他们的叫法，跟我们的字段对不上是常态。

#### ⛔ 三条硬的

1. **不许在代码或提示词里写死「某一列是什么」。**
   写死了机器就不问了，等于把确认这一关亲手废掉。
   反例：`引擎-认表.js` 的提示词现在写着「**门店**从第几列开始」——
   它一上来就假定那排是门店，逼着 AI 必须填个数，它就不会说「我不确定」。
   **这是要改掉的。**

2. **具体哪个客户哪张表是什么，不进这份规矩总表。**
   规矩**只有一份**、全体通用；答案**每个客户每张表一份**，存在本子里，
   而且是**人第一次用的时候点出来的**，不是预先写好的。
   （「碧源那排是点位」是答案，不是规矩——别家那排可能真是门店。）

3. **本子的键记「表头文字」，不记「第几列」。**
   Excel 的列号是稳的，**照片不是**——拍歪一格就整体错位，
   旧答案会对到隔壁列去，出一张看着正常的错单。

#### 问一次，记一辈子

记的是**单个名字 → 它是什么**，一本，全网通用：

```
万寿 → 点位     早餐 → 餐次     8/1 → 日期     大板 → 规格
```

- **可疑** → 弹一句问 → 人点 → **每个名字都记下来**
- **以后**：同样的名字再出现，不管在谁的表里、第几列、文件叫什么 → **不问**
- 客户加一个收货点 → 其余的还认得，**不用把整排重问**

⚠ **这本名单唯一的作用是「别每天问同一句」，不是为了省钱。**
问多了人就会闭眼点确认，闸等于没有。

⛔ **不许记这些**（记了反而会错）：文件名、第几列、表头文字、整张表的样子
—— 这些天天变（客户发的是「碧源0803.xlsx」「碧源0810.xlsx」），记了一次都命不中。

跟 §四「学叫法」是同一套路子，只是学的东西不同：
§四 学的是**商品名怎么对**，这里学的是**这个名字是不是点位**。

### 5.5.1 为什么推翻重来

老板 2026-08-03：

> 「我们做一张表格，让 AI 去填，并不是让他猜，**所有东西都不要有猜的成分**。
> 　你读完这张图的信息一定要分清楚，哪个是点位，哪个是品名，哪个是数量，
> 　哪个是单位，还有哪些是它的抬头、分店名称、经手人。」
> 「他每一个客户的表格形式都不一样啊，能不能解决这个问题？」

**老路子**：图 → AI 写成一坨文字 → 正则从平铺的字里**倒推**结构。
倒推就是猜。AI 明明看得见「豆腐串」在「商品名」那一列底下，
可我们让它把版面压成文字，那点信息在交出来的一刻就没了。
于是每来一家新写法就得加一条正则 —— 雅食乐、御口福、惠丰、祺之盛…
堆了十几条还在漏，而且加这条常压坏那条。

**新路子**：图 → AI 往一张**定死的格子表**里填 → 我们只做校验和搬运。

> 格式有无数种，装的信息就那么十几样：谁下的、什么货、多少、什么单位、
> 送到哪个点、什么时候要、有什么交代。**定死的是信息的种类，不是格式。**

手上 11 种写法全都往同一张表里装，一个装不下的都没有。

### 5.5.2 格子表（`引擎-读结构.js`）

抬头：客户 / **供应商**（我们自己，单独一格，免得跟客户串）/ 单号 / 部门 /
经手人 / 下单日期 / 送货日期 / 电话 / 结算方式 / 线路 / 页码 /
**表上条数** / **表上数量小计**（后两个是校验码）

每一行：品名（客户原话）/ 规格 / 单位 / 单价 / **表上总数**（校验码，不是答案）/
**金额** / 备注 / 数量 / **分布[]**（点位号、点位名、数量、单位、备注）

**三条铁律**（写在提示词里，读结构那边再验一遍）：

1. **不许算术** —— 「表上总数」只能照抄，真数由我们自己加分布得出，两个一比才露馅
2. **不许猜** —— 数字看不清写 `?`，走橙色 + 空数量框 + 不许出单那一套（见 5.5）
3. **没有就别写** —— 读不到的字段整个省掉，不要填空字符串装作读到了

### 5.5.3 五道校验（只报不改）

| 查什么 | 抓什么 |
|---|---|
| 分布之和 ≠ 表上总数 | 某一段的数字读错了 |
| **金额 ÷ 单价 ≠ 数量** | 没有合计列的单子（碧源）只有这条兜得住 |
| 读到的条数 ≠ 表上任务数 | 漏了一整行货 —— 最贵的错 |
| 全单数量 ≠ 本页小计 | 广垦那张自带 |
| 「第1页共2页」只给了一页 | 后面那页整页没进来 |

### 5.5.4 「表怎么读的」—— 机器自查，不是给人看的

老板 2026-08-03：「**我还要去确定怎么读的吗？**」——**不用**。

AI 填表之前先报一句「这张表我是怎么读的」（版式、每列是什么、校验码在哪）。
这段是**给机器对账用的**：对得上一声不吭，自相矛盾才弹出来。

查四样：两列都当成数量 / 有列它自己说拿不准 / 说是矩阵表却没填分布 /
说有校验码却一个没填。

⚠ **只报跟钱有关的列**（商品/数量/单位/点位/规格/价钱）。
2026-08-04 批量跑 50 家真单，8 条告警里 6 条是这条在吵 ——
「序号」「类别 C/A」「H-14 线路号」拿不准，跟开单一分钱关系都没有。
**狼来了喊多了，真出事那次也没人看。**

### 5.5.5 Excel 只让 AI 认表头 ★（`引擎-认表.js`）

老板 2026-08-04：

> 「我肯定要最便宜的方法。很烧 token 的这种以后就直接报错，不要浪费钱。」
> 「那个 Excel 表，我打开看见的只有那一个表格，我无法判断客户发过来的东西
> 　有哪些是我不需要的，所以我把整个拖给你。」

—— **挑哪张表是工具的活，不是他的活。**

Excel 的格子浏览器本地读得一清二楚，**没必要让 AI 把几百个数字再抄一遍**
（它抄才最容易抄错，还最烧钱）。三步：

1. 本地把每张表压成**骨架**（表头 + 头尾几行 + 行列数）
2. AI 只回一句**读法**：用第几张表、哪列是什么、哪几张是重复的
3. 本地照读法把整张表**摊平**成格子表

实测 `4号豆制品.xlsx`（41 商品 × 41 门店、一个工作簿 6 张表）：

| | 进 | 出 | 钱 |
|---|---|---|---|
| 全量抄给 AI | 39,840 | 46,335 | ¥9.8 |
| **只认表头** | 5,060 | 514 | **¥0.27** |

**41 个商品、合计 967.45，跟表上一分不差** —— 而且更准，数字没经过模型的手。

图片没办法（本地拿不到格子），照旧全量识别。Word 里的表格同理走这条。

⚠ **一个工作簿常装着同一张单的好几种看法**（透视表 + 明细清单）——
**只能用一张**，重复用就是数量翻倍。优先用一行一条的明细清单。

### 5.5.6 花钱之前先拦 / 失败不许出假单

- 发出去之前先估 token，太大就**不发**，报一句并给「我知道，照样跑」的出口
- **识别没跑成，就别装作跑成了**：原文照样放回框里（一个字不丢），
  但**不自动解析** —— 2026-08-03 余额不足那次，代码自动解析出一屏
  673 行的假单（时间戳当商品名）。**一张看着像模像样的假单，比什么都不出更糟。**

### 5.5.7 老路子留着不删

`引擎-解析.js` 那套正则**留着当退路**：格子表剥不出来、人手改过原文、加过单，
都退回它。页面上写明**「读法：照格子表读的 / 退回文字倒推」**——
出问题第一句要问的就是这个。

守着的：`测试-读结构.js`（74 项）、`测试-认表.js`（43 项）

---

## 五点六、点位册 ★（2026-08-04）

老板 2026-08-03：「点位号你只用识别出来就可以了…也可以做一个点位册。」
老板 2026-08-04：「做那个点位册的时候，**顺便也可以让他把对照表再完善一下，
再顺便学习一下**。」

**治的毛病**：全单只有「点位」这一栏没人校验 —— 商品名读错配不上价格库会报存疑，
价格读错金额对不上；点位是纯照搬，读成什么印什么。于是同一个点每识别一次换一个写法：

```
A031 沁园庭#教师 ↔ 沁园@#教师
B173 嘉家贝#员工 ↔ 塞家岛#员工
A829 铂轩#客餐   ↔ 铂骊#客餐
```

分拣的人照着单去册子上找，找不着。

**办法**：跟学商品叫法**一模一样、同一个时机** —— 你点「确定订单」，
叫法学一批、点位记一批，一起存服务器，换电脑不丢。

> ⚠ **2026-08-04 老板推翻了原来的做法**，下表已经是改过的：
> 「它的作用并不是要用这个点位 —— 它可能也会改，**B189 现在是利丰，下一次就不一定是利丰**。
> 　所以这个点位它的作用，你不能给它搞混。**它作用就是复制粘贴。**
> 　你如果用它能识别到更多的东西就用；用不到就不用。**但是一定不能识别错。**」
> 「**以后不要弹出来了，直接记住就可以了。**」

| 情况 | 怎么办 |
|---|---|
| 号和名都对得上 | 直接过，不打扰 |
| 号对得上、名不一样 | **只记录，一个字都不改**（单据上写的才是这次的事实） |
| 号在册子里没有 | **不弹窗**；确定订单时静默入册 |
| 同一行里同号出现两次 | **弹出来问**（江云 8/3：A056 被看成 A002）—— 这是真错，不是册子的事 |

**为什么改**：点位号 → 名字**会变**（客户改名、换点）。册子里记的是"上次见到的样子"，
不是"正确答案"。拿旧名字去覆盖这次读到的名字 = **静默认错**，直接违反 §0.1。

**一家一本，照那家的样子存** —— 江云有号有名、祺之盛只有号（`E028`）、
广垦只有名（`工商银行佛山分行/员工餐`）、一分利纯数字（`1337`）。有什么存什么。

⚠ **号和名都不许动**（2026-08-04 起）。
以前这里写的是「只动名字不动号，点位不参与算钱所以自动归一是安全的」——
**那句话已经作废**：点位虽然不算钱，但它决定**送到哪儿**。
客户真把 B189 改了名，我们"自动归一"回旧名，分拣就按旧名去送 —— 错得比算错钱还难查。

守着的：`引擎-点位册.js`（`顺一遍` 只看不改）、`测试-点位册.js` 33 项，
其中 3 项专门钉「**单据上的写法一个字都没被改**」。

2026-08-04 批量跑 50 家历史单，已攒到 **26 家 / 253 个点位**。

守着的：`测试-点位册.js`（30 项）

---

## 五点七、改单 / 加单，分两种 ★（2026-08-04）

老板：「要做一个加单的按键，**加了单才冻住。不加单的话，可以修改**。
有修改订单跟加单，分两种情况。」

| | 什么时候 | 行为 |
|---|---|---|
| **改单**（调出来默认） | 价挑错了、数量要改 | 每一行都能改，重存，单号不变 |
| **加单**（按按钮才进） | 客户微信里又补一张 | 当下这些行**立刻冻住**，新的接在后面 |

以前「调出来一律冻」把两件事混成一件 —— 碧源那张单挑了「九龙水豆腐 ¥2」，
单价纹丝不动（`rematch` 绕开冻行），教是教会了，这一行一个字没变。

⚠ **人亲手在下拉里挑了商品，那一行一定解冻** —— 他亲手挑就是他要改，
不是「悄悄改单」，两回事。

---

## 六、上线

```
node 全部检查.js        # 22 项、约 800 条，全绿才准传　（不花一分钱，不调 API）
```

传网页（**11 个文件**）：双击桌面「部署包」里的 `2-上传网页.bat`
传后端（`后端-server.js`）：**那个 bat 不管后端**，要单独 scp + 重启 `kaidan-api`

线上：https://choeyy88.com　账号 `laowu` / `LBr49jHUZXpSpG`　VPS 187.124.137.49

---

## 七、还欠着的

| 事 | 影响 |
|---|---|
| **重导观麦价格库** ← 最要紧 | 已经撞到两回：轩宝缺「尝元韧豆腐（5斤）¥7」、永辉「鲜圆拉面」¥6 实际 ¥5.60 |
| 观麦重复品清理 | 中豆泡/薄油片 同名同价两条；鸿益 小板/花板/中板 各有两个价 |
| 两套换算合成一套 | `数据-换算.js` 8 条手写的（匹配前跑）+ `一份几个/换算比` 自动的（匹配后跑），概念重叠 |
| 闸门顺序没有测试钉住 | 7 道闸门的先后只写在注释里 |
| 工厂 6 个问题 | 见 `去工厂要问的.txt` |
| 李帅 S2951（43 张单） | 整家按公斤下单、价格库按斤，要工厂确认是不是 ×2 |
