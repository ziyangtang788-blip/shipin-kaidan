# 交接 · 2026-08-18

## 一、现在什么状态

- **线上 = 本地 = `v20260818k`**，校验码对过（`25202e7a…`）
- **GitHub 落后 9 个提交**，推不上去 —— 凭据失效要重新登录。
  代码全在本地提交好了，不影响用。开新窗口后跑一次 `git push origin main`，
  它会弹登录框。
- `全部检查.js` 24 套全绿；66 张真单 0 回退；一条静默认错都没有。

## 二、今天定死的那条底层逻辑（老板反复强调的）

> **一张表里，只抓四样：什么东西、有多少、什么单位、送去哪里。**
> **每一样只从它自己那一格取。备注、规格里写什么，一概不许动这四样。**
> **仓库里有这个品 + 这个单位 → 直接用；没有 → 弹出来让人自己选。**
> **系统一步都不许替人换算。**

老板原话（8/18）：
- 「不要不要不要，就是按照他下单的单位来，不要搞复杂了。」
- 「数量只有那一列的才是数量。其他的规格型号啊、备注啊，不要去管它里面的内容。」
- 「框里的字锁掉，我从来没有动过框里的字，我只需要改下面的表格里面的某个东西。」
- 「加单是两张表不一样，不能添加。让他新开一个，或者手动添加。」
- 「我导出的图片或者文件，要有整行合计的体现，而且要比较明显的那一种。」

## 三、两条路（问清楚了，别再猜）

分水岭只有一个：**有没有「格子」**。

| 进来的东西 | 走哪条 |
|---|---|
| Excel / Word / 图片 / PDF / CSV / TXT | **格子路**：抓取 → 读结构 → 四样表 |
| 直接粘进框里的文字 | **文字路**：parseOrder 从字里猜 |

图片和 PDF **不是单独的路** —— OCR 完变成格子，跟 Excel 汇到一起。

格子路里有 3 档（只影响花不花钱）：① 抓取自己认 ② 读法本套用 ③ 问 AI。
三档出来的都是同一份结构。

**⚠ 两条路共用最后那道 `收尾`**（备注为准 / 别的货 / 单位拆开）。
广垦是图片、走格子路，照样冒出重复行 —— 就是共用那段里
「给文字路写的猜法」把格子路带歪了。今天修了「备注改数量」那两条，
**其余的还没逐条过**。

## 四、今天改完上线的 17 件

按提交顺序（`git log --since="2026-08-17 20:00"`）：

1. 名字改回**尝元**，版本号收成一个常量
2. **品名列拿价格库当尺子**，不再猜位置（广垦 7 行错 → 8 行对）
3. 剩下那列**问一句「是不是点位」**，答完按名字记住
4. **合计行不许当成货**（广垦金额翻倍）
5. 单位框缩小
6. **合计行改成靠算术认**（上一条是补丁，这条才是根子）
7. 「手动加的」行**要写客户原话** + 体检钉住
8. 删掉我自己埋的一道闸（点位列被当噪声）
9. **说明里的数字不许动数量**（8部 一张单少 103 斤）
10. **备注里的数不许另开一行**（广垦多算 349.60）
11. **体检-别的不许动这四样.js**（65 种写法 × 两种表形）
12. **数量格只当数字读**（13+100、全角１００）
13. 路不许跳（原版是锁框，后来撤了，见第 15）
14. **导出的纸/图片/CSV 都带「整行合计」**
15. **不许系统折板** —— 客户写什么单位就按什么单位
16. 撤掉原文框 readOnly（**锁上之后文件拖不进来**）
17. 把被误删的第 15 条找回来 + 版本号改完当场验

## 五、还没做的

| | 什么 |
|---|---|
| 1 | **学对照记的是脏名字**（品名后面粘着规格）→ 规格一变，教过的要重教 |
| 2 | 显示的品名带着规格，不干净（配货不受影响，会拿纯品名再试一遍） |
| 3 | **点位那几条规矩没验过**（从品名尾巴摘、用上一行小标题补） |
| 4 | `收尾` 里给文字路写的其余猜法，没逐条过 |
| 5 | GitHub 要登录一次 |
| 6 | **「商品选不了」老板反馈过，我没确认修好** —— 开窗口第一件事让他再试一次 |

## 六、⚠ 今天踩过的坑，别再踩

**1. 版本号别用写死的字报成功**
```js
console.log('版本 v20260818i')   ← 写死的，替换失败也照样打印成功
```
连着两次替换其实没生效、文件停在 h，我却以为上线了。
**改完必须把文件读回来验**，连部署脚本会 grep 到的第一个版本串一起对。

**2. 被拒绝的命令，可能已经执行了一半**
老板拒了一条 `git stash && git checkout <旧提交> -- …`，
可 `git checkout` 那半截落到了磁盘上，整条「不许折板」被冲掉，
我还在冲掉之后的状态上又提交了一次。
**被拒之后先 `git status` + 抽查关键改动还在不在。**

**3. 加闸门只量一头 = 白量**
我加过一道「防噪音」的闸（`几格 ≤ 货行数 × 1.5`），
只量了「拦掉多少噪音」，没量「漏掉多少该问的」。
拿掉它 66 张真单一次不多不少 —— 它唯一的作用是误伤真单。
**加任何闸，两头都要量。**

**4. 测试可能绿得不对**
有条测试只查 JSON 里有没有「银行」二字，可人真正看到的话是
「核对一下数量」，答非所问，照样绿。**判据要落在人看得见的那句话上。**

**5. 体检也有盲区**
第一版「别的不许动这四样」只造了「备注单独一列」那种表，
8部 那种「一格挤一串」一条都验不出来。**两种表形都得造。**

**6. 注释里别写完整的 `<script src="...">`**
`全部检查.js` 靠正则扫标签，注释里出现一个假的，它会当成真文件去找。

**7. 锁框那种小动作要包 try**
假 DOM 里没有 `style`，一个 TypeError 把整个「收下格子表」打断，
货都没进原文框。

## 七、CLAUDE.md 要改的

那条死规矩现在**作废**了：

> ~~**斤转板**：整除优先，除不尽按斤。跟观麦冲突时以这条为准。~~

改成：

> **不许系统折板**：客户写什么单位就按什么单位。仓库有这个品+这个单位就直接用，
> 没有就弹出来让人选。（老板 2026-08-18 推翻 8/2 定稿）

代价老板自己选的：488 条真实下单词里有 21 条「客户写的单位 ≠ 库里的单位」，
**教一百遍也每次都问**。测试里钉死了：这 21 条必须全是单位对不上那种。

## 八、怎么快速验

```
node 全部检查.js                    # 24 套，全绿才许上传
node 体检-别的不许动这四样.js        # 65 种写法，一个都不许带歪
node 体检-会不会认错.js             # 静默认错必须 0
```

部署：双击 `桌面\部署包\2-上传网页.bat`（或跑 `push-all.ps1`）。
末尾那句「⚠ 线上版本跟本地对不上」是**假警报**——自检 curl 的网页密码过期了，
看 scp 退出码和 `接口8788 = 200` 就行。
要真验，SSH 上去 `md5sum` 跟本地比（钥匙是 `~/.ssh/kaidan`，不是 id_ed25519）。
