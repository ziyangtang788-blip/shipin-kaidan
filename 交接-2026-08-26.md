# 交接 · 2026-08-24 ～ 26

前一份：`交接-2026-08-22.md`。

## ⚠ 先看这个：线上和本地一样，都推上去了

```
线上 = 本地 = v20260825d   （最后一次推是 bf01554）
```

工作区只剩三样没提交的**试验/调试文件**，跟线上无关：
`试-学表格-甲/乙/丙.html`（被老板否掉的三个界面样子）、`调试-*OCR*/`（真单 OCR 存档）。

⚠ **推不上去的时候**：8/26 撞过一次，`git push 服务器 main` 报「correct access rights」，
其实是 **VPN 开着，端口 50000 整个不通**。老板换了个节点就好了。
先 `timeout 20 ssh -i ~/.ssh/kaidan -p 50000 root@187.124.137.49 'echo ok'` 判一下，
不通就让他换节点（**别让他关 VPN —— 关了他就用不了 Claude Code**）。
实在不行先 `git push origin main` 备份到 GitHub，代码不会丢。

---

## 零、⛔ 老板 8/21 那条总命令还在

> **「我不让你做，不要做，以后任何事情。」**

叫你查就只是查，查完说根子和改法，**停下等他点头**。判据：他这句话里有没有「改／修／弄好／推上去」。

**这三天新加的两条，一样要记住：**

- **8/23**：「我现在主要是跟你纠的，这个是**学习功能**的东西，并不是说具体某个学习的内容。这个你要分得清楚。」
  → 他在纠**机制**（什么时候问、话怎么说、学完存哪），不是纠某张表读得对不对。
  别再往「某张表哪一行错了」上跑，那是机制做好之后拿真单磨的活。
- **8/23**：「我给你的卷子答案**不能当学习的内容**，只不过是试这个功能。」
  → `卷子-底料.json` 只用来**检验**，它的答案一个字都不许写进规矩里。
  照答案改代码 = 卷子全绿、换张新表照样错。

---

## 一、这三天的主线：**学表格重做**

老板 8/23 从头提的：「新表格、或者表格改一点，就识别不出来、报错。」

### 1.1 先做样子，做了四版才对

| 版本 | 他的原话 | 结论 |
|---|---|---|
| 甲/乙/丙三种界面 | 「都不行，太乱了」 | 全否 |
| 整条流程一屏 | 「差不多是这个流程」 | 定了 |

**定下来的形状（他亲口确认过的）：**

1. 读得懂 → **直接出单，一句不问**
2. 读不懂 → **一次只问一件**，两个按钮（对 / 不是）
3. 你答一件 → 马上重读 → 报一句还剩几处
4. 问完 → 点「记住」
5. **下次同一家发同样的表，一句不问**

**他反复纠的几条（都已落地）：**
- 「不要给我看符号」→ 不显示 `按 + 分`，显示**分完长什么样**
- 「这种没有问的必要，肯定是分成 9 份的」→ **账能证明的一句不问**
- 「问哪些要问，你得衡量清楚」→ 没得选的也不问；已经用上的列不许再问
- 「一整列，你凭什么不问就扔掉」→ 只有明摆着没用的（序号/单价/金额/空列）才扔

试验页还在，能开：`试-学表格.html`（15 张真单）、`试-整体界面.html`（放进开单台的样子）。

### 1.2 并进真系统，分了两半

- **8/24 上半**（`86d1212`）：**这一格该分成几份** —— 新的第 3 步（`id=stp-learn`），
  单独一块地，不再挤在会被「回头核」盖掉的 `ocr-msg`。老弹窗 `问怎么切` 留着当退路。
- **8/24 下半**（`b7cf0b5`）：**这一列是什么** —— `选项.列读法`，答完按
  【客户 + 这张表长什么样】记在 `OV.列读法`，**后端白名单已带上**（漏了会被静默抹掉）。

拿 15 张真表量过：**9 张一句不问**，5 张各问 1~2 列，读出来的货一格没变。

---

## 二、真单一张张撞出来的洞（都已上线）

| 日子 | 撞出来的 | 根子 | 提交 |
|---|---|---|---|
| 8/24 | 朱鲜生明细老读不完（57板读成35板） | `if(!segs.length)` —— 通用切出**半截**就不试学过的了。**切出半截比切不出来更糟，它不吭声** | `e90735d` |
| 8/24 | 阳农点位栏印着一整串原文 | **一段的破折号明细**切不开（尖括号 8/19 补过、冒号 8/22 补过，破折号一直漏着，第三次） | `1af245f` |
| 8/24 | 学习为什么没起作用 | **没有一道网在看点位**。点位里还带着「几斤几板」= 这一格没切开，得喊 | `e968f9c` |
| 8/24 | 「客户编码」整列点位没了 | 词表里 `编码` 一个词把**客户编码**也当商品编码吞了 | `621a2d4` |
| 8/25 | 补货单纯数字点位又丢 | 靠位数分不开商品编码和点位编码 → 改靠**一样货几个码**（商品编码一样货一个码，点位编码一样货几个点几个码） | `12e0266` |
| 8/25 | 补货单还是没点位 | 光露出来不够，**得标成「要问点位」**，否则被当硬伤 → 整条近道作废 → 转去问 AI，而 AI 那条路上没这一问 | `3386d47` |
| 8/26 | 碧源好好一张表被丢给 AI | **假点位号**：「(271-**5天内日期**)12斤」里的 `271-5` 被当成点位号。收窄成只认朱鲜生那种（点位排一堆、数量排一堆） | `bf01554` |

**这一串全是同一个形状**，值得记住：

> **引擎记了账，可页面没下文 → 整条路作废 → 转去另一条路 → 那条路上没这一问 → 问题无声无息消失。**

---

## 三、★ 现在卡在哪（下一件事，老板已经问到脸上了）

8/26 他拖了 `桌面\555.png`（祺之盛那种「明细写在下一行」的表），问
**「回归原点，我的学习功能去哪儿了？」**

跑了真 OCR（存档在 `调试-真单OCR/555.png.ocr.json`，**别重复花钱**），量出来：

```
OCR 抄得一个字不差
【这一列是什么】 近道摊出 0 样 → 整条路作废 → 这一问根本没机会跑
【这一格怎么分】 读不懂 16 个 → 会问，但问的是「格子怎么分」，
                 而这张单的病是【那 16 行明细整行没被当成明细】—— 问了也治不好
结果：16 样货、数量全是「?」、明细 16 行全丢
```

**根子（已经跟他说了，他让先写交接）：**

> **学习是挂在「某条路的某个信号」上的，不是挂在【结果】上。**

**改法（他还没点头，别自己动手）：**

> 不管走哪条路，**最后看结果** —— 读出来的货有数量吗？有送货点吗？没有就问。

这一改，8/24～26 撞的这几张（补货单、碧源、555）**会被同一条规矩叫住**，
而不是一张一张打补丁。

---

## 四、还欠着的

1. **★ 学习挂到结果上**（上面第三节，最要紧）
2. **「明细写在下一行」的表读不了** —— 8/22 §4.3 就记着，一直没做。
   `摊平` 的续行分支只接**光数量**的续行（厨鲜达那种），不接**一整串明细**的续行。
   555.png 就是这种。顺带：「155串(155串)」这种括号重复的写法数量取不出来。
3. **54 家客户没有商户编号** —— 导观麦会报「商户异常」。
   - 已经从订单头里捞回 2 家（东方市场 S3748242、王府井 S3724766）+ 鸿盛 S3946265
   - 剩下 54 家那批订单里从没下过单，**只能去观麦爬**
   - 爬取脚本写好了：`桌面\爬观麦商户-粘到观麦Console.txt`
   - ⚠ **8/26 试过，`/customer/list` 是 404**，另外 15 个候选地址也都不通。
     报价单那个接口是好的（`/salemenu/sale/list`，187 张），但**报价单对象里没有商户编号**。
     下一步：让他在观麦「客户管理」那页开 F12 → 网络 → 刷新，把那条真实请求的网址发回来。
4. **价格库是 8/10 的** —— 下载夹里两份 CSV 都是 8/10 的，跟现在的库一模一样（5893 行/181 家），
   **重跑没有任何变化**。要更新得让他先去观麦重爬（`桌面\爬观麦价格-粘到观麦Console.txt`）。
   ⚠ 更新**不会**冲掉学过的：学过的对照存的是**商品编码（D 码）**不是位置，码没变就还认得；
   码变了/货没了的会成死键，`体检-学过的还对吗` 会报。
5. **近道那 5 张「剩字」白花 AI** —— 江云、朱鲜生、望家欢、聚农、碧源，
   近道被「剩字」判成硬伤 → 转去问 AI。可「这一格怎么分」正好能接住它，
   只是**只接在 AI 那条路上，没接到近道上**。接了这 5 张就不用再花 AI。
   ⚠ 风险要说清楚：这 5 张现在走 AI 读得好好的，改完会改走近道，**读出来可能会变**。
6. **更早欠的**（一条没动）：整行往右挪一位（品昌/御口福/广晟）、
   空格隔开的明细教不会（御口福「1盒\*A【K53】 2盒\*B【515】」现有规矩表达不了）、
   合并/分单、8/18 夜那批（轩宝死键、跨单位教不会、老式 `.doc` 拒收）。

---

## 五、这三天踩的坑

**1. 改了引擎却没换缓存戳 —— 等于没上线**
8/24 推完 `引擎-认表.js`，`?v=` 还停在旧号，老板浏览器接着吃缓存里的旧引擎。
**每次改动都要改版本号**（CLAUDE.md 写着的），他 8/24 还特意说了「**版本号按照日期来**」。
现在是 `v2026MMDD` + 字母：`v20260825d`。

**2. 测试守写法不守意图，这三天又栽两次（第四、五次了）**
- `/剩>0\)\s*问怎么切/` —— 换个入口就红，闸门根本没动
- **注释里写到函数名，被当成调用** —— 两次都是这个。**比之前先剥注释**：
  `H.replace(/\/\*[\s\S]*?\*\//g,"")`

**3. 收窄要拿真表量，不能凭感觉**
「一整列不问就扔掉」那条，一开始把**光数字**也算进去 → `测试-点名` 当场抓到误伤
（两行的表里「1、2」还没到「≥3 行才算序号」那道闸，被当成可能的点位问出来）。
后来又误伤碧源的「商品编码」（a00795 过不了「像编码」那道，它只认纯数字）。
**每收窄一次都拿 15 张真表重量一遍**，脚本在 scratchpad 里。

**4. 别照图猜 OCR 原文**
8/26 碧源那次，照截图重建原文试了三遍都复现不出来，白花时间。
**要么让他把文件存到桌面我真跑一遍**（`跑真单.js`，会存档不重复花钱），
要么让他按 F12 抓原文。**猜不出来就别猜。**

---

## 六、这三天新增的测试（都已挂进 `全部检查.js`）

```
测试-学分法.js            36 项   账能证明的不问、没得选的不问、分不出来不硬凑、
                                学过的排前面拿总数当裁判、教错了能重教
测试-新建品要有编号.js      12 项   自己编的假码不许填进观麦「商品ID」
测试-客户编码是点位.js      10 项   「客户编码」是送货点，不许当商品编码静默扔掉
测试-这一列是什么.js        19 项   一整列不许不问就扔掉；教过的下次一句不问；
                                纯数字点位编码靠「一样货几个码」认出来
```

老测试里加的：`测试-一行多点位走AI.js` 19 → 23 项（假点位号那条）、
`测试-选完客户重读.js` 改成守意图。

**三层验证网每次都跑，一次没跳过**：`全部检查.js`、`真表快照.js 比`（35 张真表）、
`改前改后比一比.js 比`。**这三天 35 张真表一格都没变过。**

---

## 七、手头的工具（scratchpad 里，下个窗口可能要用）

`跑真单.js`　拿真图跑 OCR → 格子表 → 两条路各读一遍，摆出点名账。
OCR 结果存 `调试-真单OCR/`，**同一张不会重复花钱**。
用法：`node 跑真单.js "C:/Users/李正/Desktop/555.png"`
