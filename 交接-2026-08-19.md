# 交接 · 2026-08-19

前一份：`交接-2026-08-18-夜.md`。那份里「52 个文件没提交」「API 额度锁到 9/1」两条**都已经解掉**，别再照它行事。

**线上版本 `v20260819k`**，今天推了 6 次，每次都 ssh 上服务器逐个文件核过。

---

## 一、今天修的 6 件（都上线了，都带回归）

按撞到的顺序，全是**算错钱**或**闷声出错**：

| | 撞到的 | 根子 | 现在 |
|---|---|---|---|
| 1 | 交叉表该 164 算成 **1.2 亿** | 这条路从来没产生过「收货方从第几列到第几列」，摊表默认从第 2 列吃到表尾 | 让 AI 报**两个轴**，正反一次覆盖 |
| 2 | 「20大」的**大**被整个吃掉 | 数字后面跟的字直接丢 | 不是单位就进**备注** |
| 3 | 好好多按公斤下单 | — | 数量自动 ×2（豆腐串不动） |
| 4 | 永辉「教一百次学不会」 | **系统不认识 KG 这两个字母**，KG≠公斤，每单都判存疑 | KG/kg/千克 → 公斤 |
| 5 | 朱鲜生四行多开 **¥443** | 明细里的点位号「30-2」被当成数量；表上有总数列，可校验网**只认一个字段名** | 补网 + 提示词写明 |
| 6 | 碧源「(256#05-1砖)**0斤**」被静默扔 | 8/2「填0=今天不要」被当成全局规矩 | **老板 8/19 划线：那条只管文字路** |

### 第 6 条是今天最重要的一条规矩

老板原话：
> 「做个补丁，专门针对这种表格。**只要备注明细里面有的，全部都不能扔。**
>   我之前说的 8 月 2 号，它是针对于**文字那条路**的，并不是针对这个表格这条路的。」

实现：`收尾(out, 从格子表)`，`引擎-读结构.js` 传 `true`。
- 文字路 → 0 照旧扔（8/2 一个字没动）
- 表格路 → 0 段留着，挂 `零数量` 记号，页面照「没看清」那档办：**行摆出来、商品能选、数量框空着、不要就点删**

⚠ 他**明确不要提醒条**（我先做了一版提醒条，被否）。测试里钉了「不许再加回去」。
⚠ 边界收窄过两次，**别再放宽**：① 只管有明细列的行（8/3 云吞皮那种照旧丢）② `qtyGuess` 的段不留（会盖住 8/4「0 品必须报」那条硬线）。

---

## 二、今天新增的 5 套回归（已挂进 `全部检查.js`，现在 49 套）

```
测试-交叉表认列.js        38 项   交叉表两个轴，正反都覆盖
测试-客户量纲.js          26 项   好好多 ×2，含幂等（跑四次还是 6 不是 48）
测试-KG就是公斤.js        23 项
测试-点位号不许当数量.js   12 项
测试-零数量要摆出来.js     27 项   含「页面真摆成一行」+「不许再加提醒条」
```

**三层验证网每次都跑，一次没跳过**：`全部检查.js`、`真表快照.js 比`（35 张真表）、`改前改后比一比.js 比`。今天 35 张真表**一格都没变过**。

---

## 三、还欠着的

### 3.1 合并单 / 分单 —— 讨论到一半，老板说「等下讨论」

已经问清楚的：
- 合并 = **同一个商品各点位合成一行，还是一张单**（不是拆成多张单）
- 页面也要合并，**所见即所得**（他选的）
- 挂在**客户**身上，这家合并那家分单

我已经摆出的三个坑（他还没回）：
1. 有「?」的段不能直接相加 → 合并行也得标没看清
2. **点位减价不同的不能合**，不然金额没法算
3. 合并会盖住「点位号读错」（他 8/3 自己踩过）—— 但对合并单的客户不算事，他们本来不看点位

还没问的：**先给哪几个客户开合并**。

### 3.2 早先那两个原型，没并（老板说先别动）

`试-文字切成表格.js` + `试-文字切成表格-验.js`（51 项全过）。
把粘贴的文字切成表格走格子路。**问他日常怎么开单，他说主要拖文件和发图片**，所以这东西对他日常碰不到，没并。

⚠ 那次查明的一条：**Excel/Word/CSV 早就统一成格子了**，图片他定了不翻。所谓「统一格式」真正剩下的只有粘贴文字这一件。

### 3.3 昨天那份欠的，今天一条没动

`交接-2026-08-18-夜.md` §5.2～5.5 那些（收尾里剩下的猜法没逐条过、轩宝 4 条死键、跨单位教不会、碧源那两行为什么存疑、老式 `.doc` 拒收、学对照记的是脏名字…）。

---

## 四、今天踩的坑（下次别再踩）

**1. 服务器时间是 UTC，跟本地差 8 小时**
我把服务器的 `08:56` 当成北京时间，据此断言「今天一条都没存上服务器」——**全错**，那其实是 5 分钟前。查文件新旧之前先 `date` 对表。

**2. 本地复现页面行为，必须先 `applyAdd`**
「新建的品」（`padd`）存在覆盖层里，不装进 `DATA` 就查不到，我因此把 3 条正常的对照判成了「死键」。照 `配送开单台.html` 的 `applyAdd()` 抄一遍再 `buildIndex`。

**3. 新增 js 要进【三份】部署脚本**
`部署包/push-all.ps1`、`部署包/deploy.ps1`、`C:/Users/Public/kaidan/deploy.ps1`。我只加了第一份，体检当场抓出来 —— 就是 8/13「检查全绿、传上去 404」那个坑。

**4. 测试别把实现细节写死在正则里**
`测试-读结构.js` 有条断言写死了 `收尾(out)`，我给收尾加了个参数，它就误判成「抄了第二份」。**测试要守意图，不是守参数长什么样。**

**5. 线上验证别用 curl**
Cloudflare 那道 basic auth 会回 401，抓到空字符串然后误报「线上跟本地对不上」（规矩总表 §六 记的密码已经不好使）。
✅ 直接 ssh：`ssh -i ~/.ssh/kaidan -p 50000 root@187.124.137.49 "grep -o '版本号 = \"[^\"]*\"' /var/www/kaidan/配送开单台.html"`

---

## 五、部署这件事，今天查清楚了

**`git push 服务器 main` 就是上线开关。** 那个裸仓库挂着 post-receive 钩子，推完自己把文件铺到 `/var/www/kaidan`，回一句「OK 网站已更新 19 个文件 版本 vXXX」。

我 8/19 一度说「git 推的是代码备份，碰不到网站」——**错的**，老板当场纠正。

- `服务器/main` ← 上游，推它 = 上线
- `origin`（GitHub）← 纯异地备份，**落后 12 个提交**，一直没推

---

## 六、老板今天说的话里，最该记住的三句

1. **「交叉表就是这样子的，是不是一个正一个反嘛」** —— 别碰上一种补一种，一次把类型定完。我第一版加的「横着一排」就是补丁思维，被他一句点破。
2. **「我之前说的 8 月 2 号，它是针对于文字那条路的」** —— 老规矩是有适用范围的，别当全局铁律。
3. **「我不要一个提示。把那一整行拉出来做一个品类，让他自己选、自己填、不要就删掉」** —— 他要的是**能操作的行**，不是**告知**。

---

## 七、一个反复出现的形状（今天撞了三次）

**AI 在「不确定的」里提了疑问 → 没人接住 → 它只好自己猜 → 猜错。**

- 交叉表：「我只把第一列指了出来，其余同类列请一并按点位处理」→ 没接住 → 1.2 亿
- 「20大」：「不清楚『大』是包装规格还是别的」→ 没接住 → 字被吃掉
- 点位号：8/18 就问过「11-10、4-1 这类数字看不出是线路号还是客户编号」→ 没接住 → 四行多开 ¥443

**AI 报的「不确定的」是有人要读的**，不是日志。后两条我已经把答案写进提示词了。
