# 配送开单台 · 交接文档

**2026-08-06。给接手的人看的，从零开始，看完能上手。**

> 这份是**全貌**。细节规矩在 `说明-规矩总表.md`（577 行，唯一权威）；
> 观麦导入那条线单独在 `观麦导入-进展.md`。三份对不上时，以**老板最近那句话**为准。

---

## 一、这是什么

广东尝元绿色食品，**纯流通配送**（不生产，不加工，没有 BOM）。做豆制品配送。

**要解决的事**：客户把订单发过来 —— 微信文字、群聊截图、手机拍的手写单、Excel、Word、PDF ——
每家格式都不一样。以前是人对着这些东西，在观麦系统里一行行手工敲单。
**一天上百张单，敲错一个数字就是钱。**

**开单台干的事**：

```
客户发来的东西  →  自动认出商品/数量/单位/点位  →  按这家的专属价配价
                →  出销售单  →  导成 xlsx  →  一次导进观麦
```

**它是补位，不是替代。** 观麦（`station.guanmai.cn`）是公司在用的进销存，继续用。
开单台只解决「手工敲单」这一段。

---

## 二、现在到哪一步了

**已经上线在用。**

```
线上   https://choeyy88.com    账号 laowu / LBr49jHUZXpSpG
VPS    187.124.137.49
```

| 能干的 | 状态 |
|---|---|
| 粘文字 / 传图 / 传 Excel、Word、PDF → 自动出单 | ✅ |
| 按客户专属价配价（177 家、5680 条价格） | ✅ |
| 按斤下单折成板计价 | ✅ |
| 学：教一次下次自己会，存服务器换电脑不丢 | ✅ |
| 点位识别 + 一家一本点位册 | ✅ |
| 订单记录、单号服务器发 | ✅ |
| 改单 / 加单 | ✅ |
| **导进观麦**（10 列 xlsx，一次进） | ✅ **本轮刚打通，本地已改、还没上传** |

回归测试：**24 组、约 1000 条断言，全绿。**

---

## 三、一张单从进到出

```
①  东西进来
    文字 / 图片 / Excel / Word / PDF
         ↓
②  变成「格子表」          ← 这一层 2026-08-03 推翻重做过，见第四节
         ↓
③  一行字 → 商品/数量/单位/点位
         ↓
④  配到我们仓库的哪个货     ← 7 道闸门，见第五节
         ↓
⑤  按斤还是按板             ← 最要紧的规矩，见第六节
         ↓
⑥  出销售单（印客户自己的叫法）
         ↓
⑦  导成 10 列 xlsx → 观麦
```

---

## 四、怎么读一张表（最难的一层）

### 4.1 为什么推翻重做

老板 2026-08-03：

> 「我们做一张表格，让 AI 去填，并不是让他猜，**所有东西都不要有猜的成分**。」
> 「他每一个客户的表格形式都不一样啊，能不能解决这个问题？」

**老路子**：图 → AI 写成一坨文字 → 正则从平铺的字里倒推结构。
**倒推就是猜。** 每来一家新写法就加一条正则，堆了十几条还在漏，加这条常压坏那条。

**新路子**：图 → AI 往一张**定死的格子表**里填 → 我们只做校验和搬运。

> 格式有无数种，装的信息就那么十几样：谁下的、什么货、多少、什么单位、
> 送到哪个点、什么时候要、有什么交代。**定死的是信息的种类，不是格式。**

手上 11 种写法全装得下。

### 4.2 格子表长什么样（`引擎-读结构.js`）

抬头：客户 / 供应商（我们自己，单独一格免得串）/ 单号 / 部门 / 经手人 /
下单日期 / 送货日期 / 电话 / 结算方式 / 线路 / 页码 /
**表上条数** / **表上数量小计** ← 后两个是校验码

每一行：品名（客户原话）/ 规格 / 单位 / 单价 / **表上总数**（校验码，不是答案）/
金额 / 备注 / 数量 / **分布[]**（点位号、点位名、数量、单位、备注）

**三条铁律**（写在提示词里，读回来再验一遍）：

1. **不许算术** —— 「表上总数」只能照抄，真数由我们自己加分布得出，两个一比才露馅
2. **不许猜** —— 数字看不清写 `?`
3. **没有就别写** —— 读不到的字段整个省掉，别填空字符串装作读到了

### 4.3 五道校验（只报不改）

| 查什么 | 抓什么 |
|---|---|
| 分布之和 ≠ 表上总数 | 某一段数字读错了 |
| 金额 ÷ 单价 ≠ 数量 | 没有合计列的单子只有这条兜得住 |
| 读到的条数 ≠ 表上任务数 | **漏了一整行货 —— 最贵的错** |
| 全单数量 ≠ 本页小计 | 广垦那张自带 |
| 「第1页共2页」只给了一页 | 后面那页整页没进来 |

### 4.4 Excel 不让 AI 抄数字（`引擎-认表.js`）

老板 2026-08-04：「我肯定要最便宜的方法。很烧 token 的这种以后就直接报错。」

Excel 的格子浏览器本地读得一清二楚，**没必要让 AI 把几百个数字再抄一遍**
（它抄才最容易抄错，还最烧钱）。三步：

1. 本地把每张表压成**骨架**（表头 + 头尾几行 + 行列数）
2. AI 只回一句**读法**：用第几张表、哪列是什么、哪几张是重复的
3. 本地照读法把整张表摊平

实测 `4号豆制品.xlsx`（41 商品 × 41 门店、一个工作簿 6 张表）：

| | 进 token | 出 token | 钱 |
|---|---|---|---|
| 全量抄给 AI | 39,840 | 46,335 | ¥9.80 |
| **只认表头** | 5,060 | 514 | **¥0.27** |

**41 个商品、合计 967.45，跟表上一分不差** —— 更省，而且更准，数字没经过模型的手。

⚠ 一个工作簿常装着同一张单的好几种看法（透视表 + 明细清单）——**只能用一张**，
重复用就是数量翻倍。优先用一行一条的明细清单。

图片没办法（本地拿不到格子），照旧全量识别。

### 4.5 数字看不清写 `?`

老板 2026-08-03：「**不要逼他写个数字，不确定的就写 ?，我们自己输入。**」

**为什么**：识别错的数字（4 看成 5、6 看成 8）规则永远挡不住 —— 字就长得像。
猜出来的数看着跟真的一样，没人查得出来。**写 `?` 不算错，猜一个数才是错。**

两道检验各管各的：

| | 抓什么 | 颜色 | 数量框 |
|---|---|---|---|
| **没看清** | 识别自己说「这字读不出」 | 🟠 橙 | 空着 |
| **总数对不上** | 各段之和 ≠ 表上总数量 | 🔴 红 | 照填，只提醒 |

人一填数量，橙色立刻消失、上面的提醒条自己少一条。**空着不许确定订单、不许导观麦。**

### 4.6 「没写数量」分两种，处理完全相反 ★

老板 2026-08-04：「**没写数量的全部当 0 不进入系统，因为客户不想改他的订货模板。**」

| 情况 | 怎么办 | 为什么 |
|---|---|---|
| 图上**明明有数字**，我们没读出来 → 标 `?` | 留住 + 硬拦，必须人填 | 我们的错，放过去就是漏货 |
| 客户**压根没写数量**（只有商品+单位） | **整行丢掉，不进系统，不报警** | 客户这次不要这货 |
| 分布里**有条目**却一段都读不出 | 留住 + 报「被吞了」 | 我们没读懂，不是客户没写 |

⚠ **第三行是命门**。2026-08-04 出过事故：AI 填了 7 段、我们一段都没认出来，
要是当成「客户没写」丢掉，**整张单会静默变空**。判据只看 `分布` 数组里有没有条目。

---

## 五、配到哪个货（匹配，`引擎-匹配.js`）

一行字进来，**按这个顺序**过 7 道闸门。改顺序前先想清楚。

```
1. matchCore     先按名字找候选（学过的 > 预置 > 精确 > 光名字 > 叫法本 > 包含）
2. 默认那头       烟干/油片不写厚薄 = 薄（老板定的生意规矩）
3. 词冲突         表上写「大」配到「小」→ 拦住，把写对那边排第一
4. 单位对不上      写「板」配到按斤卖的 → 改配同单位的；规格还对不上就问人
5. 同单位的排前面   找不到同族的，至少把单位对得上的排前面当默认
6. 斤怎么算 ★      按斤还是按板（见第六节）
7. 认一遍         并了规格反而认不出的，拿客户原话再试一次
```

**为什么「斤怎么算」放第 6**：前面几道定「是哪个货」，这一道只管「按斤还是按板」。
货还没定就去算斤板，算了也是白算。

### 5.1 什么时候敢自己拍板

**全系统唯一一份名单**（`引擎-匹配.js` 的 `ASK`）：

| 敢拍板 | 要问人 |
|---|---|
| learned 学过的 | fuzzy 光名字沾边 |
| seed 预置的 | cross 别家这么叫 |
| exact 精确命中 | gmap 叫法本 |
| default 厚薄规矩 | none 认不出 |
| byweight / fitboard / byunit 斤板规矩 | nocust 客户没定 |

⚠ **这份名单只能有一处。** 页面拿它决定弹不弹窗，体检拿它决定算不算事故 ——
各写一份的话，总有一天页面放行了、体检还当它会问人。

### 5.2 单位换算已停用（2026-08-04）★

老板：「**你换算又换算错了呢。**不要去换算这种有问题的，就找仓库有对应的产品。」

原来有一套硬编码换算（件→斤 ×10、斤→串 ×20，在 `数据-换算.js`）。
系数错一个金额就差好几倍，单据上还打「1 件」，**肉眼看不出 —— 闷声算错**。

现在：单位对不上 → 闸门4 找同单位的货；找不到 → 交给人「＋ 库里没有，新建」。
表里数据留着（工厂答复过的事实，将来建产品要用），**但绝不许接回算钱那条路**。

⚠ **斤转板不在此列** —— 那是老板 8/2 定稿的规矩，凑不整会问人，跟这套是两回事。

### 5.3 单价能反推

客户单据上自己写了单价（`阳山豆腐卜 1斤 ¥8`），可以拿来定夺，但：

- **只在本来就要弹窗时用**，不抢已经确定的答案
- **必须只有一条候选对得上价**才算数，两条都是 ¥8 就还是问人
- 一条都对不上（价格库过期）→ 当没写

---

## 六、按斤卖还是按板卖 ★（最要紧的一条）

**老板 2026-08-02 当面确认两次。跟观麦历史单据冲突时，以这条为准。**

> 「客户如果给的是斤，那么你要对应他这个产品里面有没有可以整除的斤的数量。
> 　如果没有，就是按单斤卖；如果有，就是按板卖。如果他给的是板，你就直接按板来。」
> 「**按我的规则走，不按观麦的规则来。**」

```
客户写「板/件/盒」 → 直接按那个单位算，不折不换

客户写「斤」      → 配到的本来就论斤卖？ → 一步都不许动
                  → 是板货？拿这个货所有板型去除斤数
                       · 除得尽   → 按板卖（整除优先）
                       · 除不尽   → 改按单斤卖
                       · 好几种都除得尽 → 问人
                       · 除不尽又没论斤的 → 问人
```

**实例**（裕丰：5斤板¥5.5 / 6斤板¥6.5）：

| 报数 | 结果 |
|---|---|
| 18斤 | 18÷6=3 → **6斤板 3板** |
| 15斤 | 6除不尽、5刚好 → **5斤板 3板** |
| 17斤 | 都除不尽 → **按斤价**（存疑）|

**⚠ 最容易闯的祸**：客户词里写着「斤」「散装」的，去凑板就是自作主张。
`散装攸县香干 45斤` ÷5=9 刚好整除，折成 9 包就错了 —— 观麦开的是 45 斤散装。
不加这道闸，9758 行真单里凭空多出 60 多条认错。

**一板几斤从哪读**（越靠前越可信）：规格字段 `12盒/件` → 商品名里带的 `水豆腐7斤/板`
→ 商品名括号+计价单位 `水豆腐（6斤）`+板。
**范围一律不算** —— `（14-15斤）` 算 14 还是 15 差一板的钱，宁可问人。

守着的：`测试-斤转板.js`（49 项）、`体检-斤还是板.js`（拿观麦 9758 行真单核）

> ⚠ **有一条悬着的**：8/2「能整除折板」vs 8/4「有斤就用斤」两句打架，直接改钱。
> **等工厂答复，给答复前一行不许改。**

---

## 七、学习

### 7.1 什么时候学

- **点「确定订单」= 学一遍** —— 这一单每行的「客户写的词 → 定下来的货」全记住
- 在下拉里挑一个 = 学
- 点「✓ 对，记住」= 学

老板原话：「这个确定就是学习的过程，我得把它学好。」

### 7.2 同一个词好几行

**默认统一改。** 挑完弹一句：

> 这张单里「水豆腐/板」一共有 23 行。
> 　确定 = 全部改成「尝元小板豆腐」，以后也记住
> 　取消 = 只改这一行（这个词就不记成对照了）

选「只改这一行」的意思是**这个词真有歧义**（客户漏写了（大）（小）），
那就不该记成对照 —— 记了下次还是错。

### 7.3 存在哪

**服务器。** 换电脑、换浏览器、清缓存都不丢。

开页面先跟服务器对一次 → 学到新东西 1.5 秒后自动推 → 连不上先存本机，下次连上补推。
「上次同步后本机动过哪些键」记在 `dirty` 里，同步时盖回去。

教错了能改回来：「忘掉重学」→ 本地删 + 记一笔 `gone` 送服务器，服务器照着删。

---

## 八、点位册 ★（只照搬，没有权威性）

**治的毛病**：全单只有「点位」这一栏没人校验 —— 商品名读错配不上价格库会报存疑，
价格读错金额对不上；点位是纯照搬，读成什么印什么。同一个点每识别一次换一个写法：

```
A031 沁园庭#教师 ↔ 沁园@#教师
B173 嘉家贝#员工 ↔ 塞家岛#员工
```

分拣的人照着单去册子上找，找不着。

**老板 2026-08-04 推翻了原来的做法：**

> 「它的作用并不是要用这个点位 —— 它可能也会改，**B189 现在是利丰，下一次就不一定是利丰**。
> 　**它作用就是复制粘贴。**你如果用它能识别到更多的东西就用；用不到就不用。
> 　**但是一定不能识别错。**」「**以后不要弹出来了，直接记住就可以了。**」

| 情况 | 怎么办 |
|---|---|
| 号和名都对得上 | 直接过，不打扰 |
| 号对得上、名不一样 | **只记录，一个字都不改**（单据上写的才是这次的事实） |
| 号在册子里没有 | **不弹窗**，确定订单时静默入册 |
| 同一行里同号出现两次 | **弹出来问** —— 这是真错，不是册子的事 |

⚠ **号和名都不许动。** 点位虽然不算钱，但它决定**送到哪儿**。
客户真把 B189 改了名，我们"自动归一"回旧名，分拣就按旧名去送 —— 比算错钱还难查。

**一家一本，照那家的样子存**：江云有号有名、祺之盛只有号（`E028`）、
广垦只有名（`工商银行佛山分行/员工餐`）、一分利纯数字（`1337`）。有什么存什么。

已攒到 **26 家 / 253 个点位**。

---

## 九、点位的四种写法 + 一段一行

```
(020#西餐厅)6斤+(089)3斤          雅食乐 / 碧源
2盒*KC概念餐厅(月付)【519】        御口福
广州交通管理局:7板+京都电工:3板    惠丰（观麦导出的「明细」列，必须有 Tab）
桃盛酒店，山水豆腐 3 盒            逗号前面是地方（酒店/饭堂/税局/派出所…）
```

逗号那条**只在前半段「像个地方」时才切** ——「客家豆腐，老豆腐 3斤」一个字都不许切。

**一段一行，一段都不并 ★**（老板 2026-08-03）：

> 「它同一行里有多少你就出多少行，**不要做多余的动作**。至于合并，那是以后另外要加的功能。」

原来按「点位＋减价额＋备注」分组会合并同点位的行。江云 8/3 踩到：
读出 `(A002 鹤峰#幼儿餐)3板 + (A002 鹤峰#幼儿餐)2板`，第二个号其实是 **A056**。
一合并 → 一行 5板，**「号看错了」这件事就被盖住了，反而查不出来**。
不并，两行摆着，重号一眼就看见。

---

## 十、改单 / 加单，分两种 ★

老板：「要做一个加单的按键，**加了单才冻住。不加单的话，可以修改**。」

| | 什么时候 | 行为 |
|---|---|---|
| **改单**（调出来默认） | 价挑错了、数量要改 | 每一行都能改，重存，单号不变 |
| **加单**（按按钮才进） | 客户微信里又补一张 | 当下这些行**立刻冻住**，新的接在后面 |

- 放新图/新文件进去 → **只解析新来的那段**，接在后面
- 还是**同一张单号**，重存是覆盖
- 重复商品**另起一行**，不合并相加
- 同一张图放两次 → **直接报重复**，不加进去
- ⚠「识别并计价」是**整张重算**，会解冻老行 —— 点它会先问一句
- ⚠ 人亲手在下拉里挑了商品，那一行**一定解冻** —— 他亲手挑就是他要改

---

## 十一、导进观麦（本轮刚打通）

**详细的看 `观麦导入-进展.md`**，这里只给结论。

**定稿：10 列，顺序不能动**

```
1 商户名   2 商品名   3 下单数   4 单位     5 单价
6 商品备注 7 订单备注 8 客户点位 9 商户SID  10 商品ID
```

观麦按**第几列**认，不按列名。模板里映了几列，文件就得正好那几列那个顺序。

**三个关键结论（都是实测出来的）：**

| 结论 | 怎么验的 | 解决了什么 |
|---|---|---|
| **商户SID 说了算，不认名字** | 商户名故意写错、SID 填对 → 照样落到正确的商户 | 62 家「报价单名 ≠ 商户名」的客户 |
| **商品ID 填 D 码能根治「未完全识别」** | 弹黄杠那两行填 `D3196709` → 黄杠没了 | 302 条名字互为子串的货、涉及 96 家客户 |
| **多门店必须选店** | —— | 18 家有分店的，不选就不知道开给哪个仓 |

⚠ **两个 S 码别搞混：**
```
S2984      报价单号（salemenu_id）—— 一张价目表，好几个商户共用
S3797453   商户SID（address_id）  —— 这才是「开给谁」
```

**真单实测：48 种、807.35，零报错、零黄条**，金额跟上一版靠名字导的分毫不差。

---

## 十二、数据在哪

| 文件 | 装什么 | 哪来的 | 状态 |
|---|---|---|---|
| `数据-价格库.js` | **5680 条**：177 家客户 × 商品 × 专属价 × D码/C码 | 观麦报价单爬取 | ⚠ **7/28 的旧快照，待更新** |
| `数据-商户名.js` | **124 张报价单 / 168 个店 / SID**（0 缺失） | 从观麦 9765 行订单反推 | 还差 53 家（近期没单，推不出） |
| `对照-预置.js` | 出厂就会的「客户叫法 → 我们的货」 | 手工整理 | |
| `数据-常用规格.js` | 规格对照 | | |
| `数据-换算.js` | 8 条手写换算 | 工厂答复 | ⚠ **已停用，不许接回算钱** |
| `覆盖层-线上.json` | 学过的对照、改过的价、点位册、门店 | 用出来的 | 存服务器 |

**覆盖层**是关键概念：观麦底层数据**只读**，我们所有的修改（改价、学对照、加门店）
都放在覆盖层里，重新导入观麦数据不会冲掉。改价要留痕（谁改的、什么时候）。

---

## 十三、代码地图

### 页面
| 文件 | 是什么 |
|---|---|
| `配送开单台.html` | **主页面，单文件 25 万字**，2 段 script、137 个 id |

⚠ 改页面结构前必须校验那些 id —— JS 全靠它们找元素。

### 引擎（一个文件一层，不许抄第二份）
| 文件 | 干嘛 |
|---|---|
| `引擎-读文件.js` | Excel / Word / PDF 本地解开 |
| `引擎-认表.js` | Excel 只让 AI 认表头（省 99% 的钱） |
| `引擎-读结构.js` | AI 填的格子表 → 单据行 + 五道校验 |
| `引擎-解析.js` | 老路子：正则从文字倒推（留着当退路） |
| `引擎-匹配.js` | 7 道闸门、`ASK` 名单、斤转板 |
| `引擎-点位册.js` | 一家一本，`顺一遍` 只看不改 |
| `导出-观麦.js` | 生成 10 列 xlsx |

⚠ **匹配逻辑只准一份。** 抄第二份会出现「页面死了体检却全绿」。

### 后端
| 文件 | 干嘛 |
|---|---|
| `后端-server.js` | 订单记录、单号发放、`/api/ocr` 代理（密钥不落前端） |
| `后端-装到服务器.sh` | 部署脚本 |
| `密钥-本机.js` | ⚠ **密钥只在这和服务器上，绝不进任何上传的文件** |

### 测试（24 组、约 1000 条）
`测试-*.js` 是功能回归，`体检-*.js` 是拿真单核。**`node 全部检查.js` 一次跑完，不花一分钱、不调 API。**

其中最要紧的三个：
- `体检-会不会认错.js` —— **必须 0**，否则不许上传
- `体检-斤还是板.js` —— 拿观麦 9758 行真单核
- `体检-解析漏没漏.js` —— 掉一段就是少发货

### 工具
`工具-批量跑真单.js`（拿真实单跑回归，第二参数可指定模型）、
`工具-价格对账.js`（换价格库前对差异）、`观麦-爬报价单价格.js`（重爬价格）、
`工具-生成价格库.ps1`（CSV → 价格库）、`观麦-第3步-爬订单.js`（爬订单）

---

## 十四、上线怎么做

```
node 全部检查.js          # 24 组，全绿才准传（不花钱、不调 API）
```

| 传什么 | 怎么传 |
|---|---|
| 网页（13 个文件） | 双击 `桌面\部署包\2-上传网页.bat` |
| 后端 `后端-server.js` | **那个 bat 不管后端**，要单独 scp + 重启 `kaidan-api` |

⚠ 部署包**不在项目文件夹里**，在桌面。本地改完不双击那个 bat，线上不会变。

⚠ 老板的网络到 VPS 全端口不通，走 **GitHub 加密中转 + 浏览器 Console** 部署，
命令里不能有中文文件名。

---

## 十五、⚠ 现在本地改了、还没上传的

跑过 `全部检查.js` 全绿，但**没有**双击 `2-上传网页.bat`。

| 文件 | 改了什么 |
|---|---|
| `导出-观麦.js` | 14 列 → 10 列同序；**修了「商户SID 填成报价单号」这个会开错客户的 bug**；加第 10 列商品ID（D码） |
| `配送开单台.html` | 有分店的客户，门店下拉第一项从「（不分店）」改成「⚠ 还没选门店」+ 描红 + 红字提示，选上自动消失。原来的硬拦截保留 |
| `测试-导观麦.js` | 断言跟着改（10 列钉死、SID 不能是报价单号、商品ID 必须 D 码） |
| 新增 `观麦-爬报价单价格.js` | 重爬价格（⚠ 有 bug 待改，见下节） |
| 新增 `工具-价格对账.js` | 换库前对差异 |

---

## 十六、还欠什么

### ⛔ 挡上线的

**1. 价格库重爬没做完**

`数据-价格库.js` 是 7/28 快照，观麦那边之后改过价。不重爬就一直用旧价 —— **直接错钱**。

已经爬过一次（185 张报价单 / 5737 条 / 零失败），但**对账当场抓出 bug**：

```
华丰超市大沥店  日本豆腐（件）  20 → 2000
```

5052 条"改价"全是整整 **100 倍** —— **观麦接口的价格单位是「分」不是「元」**，
爬取脚本直接当元写了。

> 要是跳过对账直接换库：生成成功、`全部检查` 全过、页面照常打开，
> 然后每一张单的金额都是 100 倍。**没有任何自动检查拦得住这种错。**

**下一步**：改爬取脚本 4 个价格列 ÷100 → 重爬 → 再对账 → 改价条数应该从 5052 掉到几十条
→ 那几十条一条条看 → 没问题才生成价格库 → 跑全部检查 → 上传。

**不要**直接拿现在这份 CSV 除以 100 —— 脚本里的错没改，下次重爬又是 100 倍。

### 不挡上线的

| 事 | 说明 |
|---|---|
| **18 家分店客户：每店一单还是总公司一单？** | 要问工厂。不定就不知道该拆几张单 |
| **53 家客户查不到 SID** | 现在这份是从订单反推的，要爬**商户列表**才能补齐（接口地址还没拿到） |
| **斤转板那条悬着的** | 8/2 vs 8/4 两句打架，等工厂答复，**给答复前一行不许改** |
| 观麦重复品清理 | 中豆泡/薄油片 同名同价两条；鸿益 小板/花板/中板 各有两个价 |
| 两套换算合成一套 | `数据-换算.js` 手写的 + `换算比` 自动的，概念重叠 |
| 闸门顺序没有测试钉住 | 7 道闸门的先后只写在注释里 |
| 李帅 S2951（43 张单） | 整家按公斤下单、价格库按斤，要工厂确认是不是 ×2 |
| 工厂 6 个问题 | 见 `去工厂要问的.txt` |

---

## 十七、死规矩（违反了就是事故）

### 0. 认错必须是 0

老板原话：「你可以让他去选择，让他去学习，**但是不能识别错误**。」

- **存疑不算错** —— 弹窗让人点一下可以接受，不许为了少弹窗去猜
- **闷声算错才是错** —— 不弹窗、直接落到单上、金额还错了，这叫事故
- 硬门槛：`体检-会不会认错.js` 必须是 0，否则不许上传

### 1. 遇到反常，拎出来问，别改规则

老板 2026-08-03：

> 「像这种情况，你不用把它整个规则改掉…你只需要把这单独的这一个东西拎出来问就可以了。
> 　**我不需要你把什么都按照规则来，我只需要你把有问题的弹出来。**」

**改规则的代价永远比弹一次窗大。** 一条规则要管几百家客户、上万行单；
为了一个特例改规则，等于拿全部去赌一个。

| 情况 | 怎么办 |
|---|---|
| 全公司通用的生意规矩 | 写成规则 |
| **某一行反常**（单位突然变了、数量对不上） | **拎出来问，不改规则** |
| 拿不准 | 先拎出来问，攒够三五次再看要不要成规则 |

### 2. 动开单台之前先问

在跑的东西不许直接改。先在桌面出独立文件跑通，老板点头才并进去。

### 3. 匹配逻辑只准一份

抄第二份会出现「页面死了体检却全绿」。上传前必须跑 `node 全部检查.js`。

### 4. 不许猜

要什么信息直接开口问，别拿三次白跑去换一个答案（观麦字段名就是这么栽的，猜三次错三次）。

### 5. 换价格库之前必须先对账

价直接是钱，人工录的价会错，自动检查拦不住。

### 6. 密钥只在 `密钥-本机.js` 和服务器上

绝不进任何上传的文件。

---

## 十八、接手第一天怎么上手

1. **先读 `CLAUDE.md`**（很短，工作规矩）
2. **跑一次 `node 全部检查.js`** —— 24 组全绿，你就有了一个可信的基线
3. **本地打开 `配送开单台.html`**，粘一段订单文字试一次，走一遍完整流程
4. **读这份文档的第五、六节**（匹配 7 道闸门 + 斤转板）—— 这两处是所有钱出错的地方
5. 要改东西之前，**先去 `说明-规矩总表.md` 找对应那一节**，它标着「哪张真单逼出来的」和「哪个测试守着」
6. 想知道老板为什么这么定，去 `记忆/`（索引 `记忆/MEMORY.md`），里面是原话

**改之前必看**：`说明-规矩总表.md` 每条规矩后面都写着守它的测试文件名。
动那条规矩之前先看那个测试，不然你会在不知情的情况下推翻一条用真单换来的结论。

---

## 十九、相关文档

| 文件 | 是什么 |
|---|---|
| `CLAUDE.md` | 工作规矩，每次开工必读，很短 |
| `说明-规矩总表.md` | **唯一权威**，577 行，按节号查 |
| `观麦导入-进展.md` | 观麦导入那条线的完整交接 |
| `观麦-导入字段清单.md` | 观麦导入模板全部字段，从后台一个字一个字抄的 |
| `说明-规则冲突清单.md` | 已知打架的规则 |
| `说明-问题总捋.md` | 问题汇总 |
| `后端设计.md` | 服务器设计 |
| `去工厂要问的.txt` | 待老板去工厂问的 |
| `记忆/MEMORY.md` | 老板拍板的原话和为什么 |

**三者对不上，以老板最近那句为准。**
