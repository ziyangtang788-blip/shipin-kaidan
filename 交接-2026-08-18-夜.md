# 交接 · 2026-08-18 夜（第三份）

前两份：`交接-2026-08-18.md`（白天 17 件）、`交接-2026-08-18-晚.md`（晚上 6 件 + 3 个根子）。
这一份管的是**上线前的清账**：什么能传、什么不能传、什么还没动、什么没并进去。

---

## 一、⚠ 上线前必须先查清的一件事

**线上到底是什么版本，现在没人说得准。**

- `交接-2026-08-18-晚.md` 写着「线上 = 本地 = `v20260818t`，18 个文件 md5 核过」
- 可我接手时，本地 `配送开单台.html` 里是 **`v20260818s`**
- 晚上那份自己也记着「`git checkout` 撤回过两个文件」

**两种可能，后果完全相反：**

| | 情况 | 后果 |
|---|---|---|
| A | 线上确实是 t，本地被 checkout 退回了 s | **线上有本地已经没有的改动** —— 直接传会把线上改坏 |
| B | 交接写得乐观，其实没传成 | 传就完了 |

**先跑这一条，别跳过：**

```
ssh -i ~/.ssh/kaidan -p 50000 root@187.124.137.49 "cd /var/www/kaidan && md5sum *"
```

跟本地 `md5sum *` 逐个比。钥匙 `~/.ssh/kaidan`，端口 **50000**（不是 22）。
⚠ 部署脚本末尾那句「线上版本跟本地对不上」是**假警报**（自检 curl 吃 401）。

---

## 二、现在本地有什么（52 个文件没提交）

**最后一个提交停在 08-18 15:52。晚上两个窗口的活全部裸在工作区里，一次都没提交。**

改动规模：52 个文件、+631 行 / −7277 行（大头是 121→71 的清理）。
`配送开单台.html` 一个文件就 **+293 行**。

---

## 三、能传的（测试验过，但见 §四）

### 晚上那个窗口的 6 条

| | 什么 | 验的情况 |
|---|---|---|
| 1 | 老式 `.xls` 能读了 | `测试-读老xls.js` ✅ |
| 2 | 人挑的商品不许被推翻 | `测试-人挑的不许被推翻.js` ✅ |
| 3 | 存疑那行显示配到哪个货 | 页面里在 |
| 4 | 新建的品／改的价存服务器 | `测试-新建的品要存服务器.js` ✅ |
| 5 | 数量不许被当点位 | `测试-数量不许当点位.js` ✅ |
| 6 | 表样指纹只认结构 | `测试-表样指纹要稳.js` ✅ |

另外改了服务器 Caddy：页面加 `Cache-Control: no-cache`。

### 今晚（夜）这个窗口的 1 条

| | 什么 | 验的情况 |
|---|---|---|
| 7 | **明细格里没写数量的那句话，不再一声不吭丢掉** | 引擎侧 `测试-明细格里的话.js` 24 项 ✅；**弹窗点击路径没人验过 → 见 §四** |

起因：8/17 朱鲜生，「盒装山水豆腐500g」「要10板，5斤/板」两句话被当空格子扔了，
而且客户自己写的总数也没算它们（19=5+10+4、87=35+2+50），**四道校验全绿**，少开 8 元。

改了两处：
- `引擎-读结构.js` 那个「空格子跳过」的 return 前面加 4 行，捡进 `只有话`
- `配送开单台.html` 加弹窗 `问要不要加`，格子路 / 图片路各接一处

**捡起来不进数量、不进金额** —— 人在弹窗里点了才会变。不点的话，钱跟改之前一模一样。

### 三层验证网，都是绿的（今晚跑过）

```
node 全部检查.js          43 套　全部通过　退出码 0
node 改前改后比一比.js 比   一格都没变
node 真表快照.js 比        35 张真表，一格都没变
```

---

## 四、⛔ 不能传的理由（只有一条，但是硬的）

**第 7 条那个弹窗，三个按钮从来没有人点过。**

「加一行货」「填数量」「就这样」——假浏览器里没有真下拉框和按钮，**自动测不了**。

原计划：本地台拖一张朱鲜生的采购表进去点一遍。
**结果卡住了：Anthropic API 撞到你自己设的花费上限**

> You have reached your specified API usage limits. You will regain access on 2026-09-01 at 00:00 UTC.

今晚我跑 47 份真单花了约 **$2.2**，可能是最后一根稻草，也可能之前就快到顶。

**两条路解开：**
1. `console.anthropic.com` → Settings → Limits，把月度上限调高（立刻生效）
2. 在页面加一个**本机专用的「重放存下来的格子表」口子**，不花钱就能试
   （顺带补上晚上那份抱怨的「识别完不留底、出事没法复盘」）—— **要动开单台，没动，等你点头**

---

## 五、需要动、还没动的

### 5.1 版本号撞车（一分钟的事，但必须做）

本地是 `v20260818t`，晚上那份交接说线上也是 `t`，**内容不一样**。
不改的话，以后没法靠版本号判断线上是哪一版。
→ 连同 `<script src="引擎-读结构.js?v=...">` 那个缓存戳一起改成 `u`。

### 5.2 `收尾` 里剩下的猜法，没逐条过（白天那份就欠着）

今晚试过一刀：让格子路不跑「备注里写了数量就以备注为准」那一段。
**砍错了，已撤回。**

**为什么砍错（重要，别再砍）**：小节标题写的是 8/2 的老规矩，可代码 8/3 之后早就不是那样了——
那一段现在只干三件事：① 备注单位对不上且段没数量 → 进「别的货」提醒人　
② 数量列已写数、备注是另一个数 → 进「备注对不上」拎出来问人　③ 数量列 0/空 → 才按备注走。
**①② 是闸，③ 是老板 8/2 定的规矩，一处「顶掉已写好的数量」都没有。**
整段关掉会连第一道闸一起关死，`测试-读结构.js` 当场变红。

已在 `引擎-解析.js:1188` 留了警告注释。

### 5.3 那张「22 件事」的表不可信

`系统自己拿主意的22件事-请勾.csv` / `请勾-系统自己拿主意的事.html`。

今晚数「系统在哪儿自己拿主意」数了三次：**362 → 515 → 22，三次都不准。**
根子一样：**从注释和小节标题推断，没有逐条读代码验证**。
这个项目注释里堆满历史（「8/10 一度删过这段」「原来是…后来改成…」），
照着读一定会把**已经修好的东西当成还坏着的**——5.2 就是当场吃的亏。

**要用它，得一条条重读代码验一遍。22 条，一条几分钟。**

### 5.4 晚上那份欠的 5 件（原样搬过来）

| | 什么 |
|---|---|
| 1 | 算术规矩原型（`试-总数对得上就是数量.js`）**没并进去**，48 张真表量过 0 张受影响 |
| 2 | 轩宝 4 条死键（指向一个不存在的品） |
| 3 | 跨单位教不会 + 「又学会 N 个叫法」那句是照行数数的，不管有没有真写进去 |
| 4 | 碧源那两行为什么存疑 —— **没查明** |
| 5 | 老式 `.doc` 还是拒收（跟已修的 `.xls` 同一场景） |

### 5.5 白天那份欠的

- 学对照记的是**脏名字**（品名粘着规格）→ 规格一变，教过的要重教
- 显示的品名带着规格，不干净
- 点位那几条规矩（从品名尾巴摘、拿上一行小标题补）**没验过**
- GitHub 要登录一次（本地领先 **10 个提交**没推上去）

---

## 六、原型跑通、但没并进开单台的

| 文件 | 干什么 | 状态 |
|---|---|---|
| `试-总数对得上就是数量.js` | 一列数加起来 = 总数 → 就是各点位数量 | 朱鲜生 27 段全对、合计 205 分毫不差；48 张真表 0 张受影响；**没并** |
| `工具-试补丁-捡回没数字的那行.html` | 今晚第 7 条的原型（老板已看过点头） | 正式版**已并**，这个留作说明 |
| `诊断-拖文件.html` | 文件拖进去逐步打出卡在哪 | 独立工具，不进主页面 |

---

## 七、今晚新查明的三件坏消息（以前没人记过）

**1. 观麦不能当答案本。**
加单、补单、删单之后，观麦的最终数跟客户原来那张表**本来就对不上**。
所以「数字读对没有」这件事，**目前没有自动验的办法**，只能肉眼。
（`C:\Users\李正\Downloads\观麦_订单明细.csv` 那份还是 7/29 截止的，也对不上 8/1 的单。）

**2. AI 从图片上漏读多少，量不出来。**
今晚量出「丢」= 1/286 ≈ **0.3%**（35 张真表，286 行进 → 287 行出）。
但这只覆盖「AI 交上来之后 → 出单」这一段。
**AI 看着原图少读了什么，没有真值就量不到。** 朱鲜生那次能发现，全靠老板肉眼对了原表。

**3. 「丢」比想的少得多，「顶包」也少。**
晚上那个窗口量过：1177 个数里 20 个（**2%**）是顶包的。
今晚量出丢是 **0.3%**。
**两半账都量过了，都很小 → 老板当时「先不做大的、一条条来」的判断站得住。**

---

## 八、上线该怎么走（建议顺序）

1. **先 ssh md5 核线上**（§一）—— 不核清楚别传
2. 版本号改 `u`，缓存戳跟着改（§5.1）
3. **提交**（52 个文件裸着太久了），顺手 `git push`（要登录一次）
4. 解开 API 额度 **或** 加那个重放口子 → **人工点一遍弹窗**（§四）
5. 点通了再 `node 全部检查.js` 一遍，全绿才双击 `桌面\部署包\2-上传网页.bat`
6. 传完照 §一 那条 ssh 命令逐个 md5 核

**第 4 步没做完，第 5 步不许做。**

---

## 九、今晚新增的工具（都不进上传清单）

| 文件 | 干什么 |
|---|---|
| `真表快照.js` | **白捡的验证网**：拿 8/18 存下的 35 张真表格子表**离线**重跑，`拍` / `比`，改引擎前后逐家比行数·段数·总量·每行品名单位数量。**一分钱不花** |
| `调试-格子表-*.json`（35 个） | 那 35 张真表的 AI 原始输出，上面那把尺子的底料 |
| `测试-明细格里的话.js` | 第 7 条的回归，24 项，已挂进 `全部检查.js` |
| `丢了多少-清单.csv` | 量「丢」的结果 |
| `系统自己拿主意的22件事-请勾.csv` / `.html` | **⚠ 见 5.3，不可信，要重验** |
| `拿主意的地方-33处.csv` / `规矩清单-待分类.csv` | 同上，中间产物 |

---

## 十、⚠ 今晚踩的坑

**1. 别照小节标题和旧注释判断代码在干什么**
5.2 那一刀就是这么砍错的。标题会过期，**代码才算数**。

**2. 数「系统在哪儿拿主意」数了三次都不准**
362 / 515 / 22，全是从注释推的。要准只能逐条读代码。

**3. 等待条件写太松，跟系统那个毛病一模一样**
`grep "跑成"` 判断批量任务跑完了——可日志**第一行**就写着「还没**跑成** 65 家」，
当场误报两次。**拿一个太松的特征下一个确定的结论**，人和代码都会犯。

**4. `ps -W` 看不到中文命令名**
用它判断 node 进程在不在，会误判「跑完了」。改盯日志里只有结尾才出现的那句。

**5. Excel 打不开 .csv**
给老板的表别只出 CSV，出一份能双击开的 HTML 更稳。
