# 交接 · 2026-08-16

## 一、开新窗口第一件事

**弹窗**：`~/.claude/settings.json` 和项目 `.claude/settings.json` 都已写死
`permissions.defaultMode: "bypassPermissions"`。新窗口开起来应该就不弹了。
要是还弹 → `Shift+Tab` 按到底，切到**红色的 `Bypassing Permissions`**。

**两条踩过的坑，别再犯：**
- ❌ 命令里带 `cd`（例：`cd "路径"; node x.js`）→ **必弹窗**。用 `node x.js` 就行，工作目录本来就是项目根。
- ❌ PowerShell 的 `Get-Content -Raw` 读中文 → 乱码。查文件一律用 Read/Grep 工具，别用 PowerShell。

---

## 二、本地测试台（这是重点，昨天卡了很久）

**开单台是两半，缺一半就会「认得离谱」，别误判成代码写错。**

```
node 后端-server.js        ← 8788，管学过的对照/图片识别/订单记录
node 本地开一个网站.js      ← 8123，网页
浏览器开 http://localhost:8123
```

**怎么确认接上了**：页面左下角写「**服务器存档**」= 对；
写「连不上服务器，暂时存本机」= 后台没开，**测出来的结果不作数**。

**根子**（8/15 挖出来的）：页面所有活都问同源 `/api/*`。只开静态站的话 `/api/health` 404
→ 页面判定「连不上服务器」→ 学过的东西退回存浏览器
→ 而 `localhost:8123` 是全新网址，里面**一条都没有**
→ 教过的 905 条客户叫法全不认得。
已在 `本地开一个网站.js` 里加了 `/api/*` → 8788 的转发，修好了。

线上 choeyy88.com 从这台机器连不上（SSL wrong version number，VPN 老问题），只能用本地后台。

---

## 三、开单台现在的逻辑

```
文字粘进来 ─┐
图片 → OCR ─┼─→ 【分两条路】
Excel/Word ─┘
      ├─ 文字路：断句 → 一行行认
      └─ 表格路：问 AI「第一样货叫什么、商品名那列表头写什么」
                 → 拿字回表里找列 → 搬数据
            ↓ 汇合
      逐行点名：每行都得有下落，没下落 → 弹窗拦住不许出单
            ↓
      认货认价（预置 + 学过的 905 条）→ 存疑弹窗问人 → 教一次记住
            ↓
      出单：订单记录 / 导观麦 / 打印
```

**跟老板的终极底层逻辑对得上的**：该有的必须有、读不懂就停、不许闷头出单、不写死列号。

**对不上的只有一条，但是核心那条**：
还是**两条路**，表格那条仍然「先搞清楚表长什么样，再搬数据」。
老板要的是「**不管它长什么样，直接把要的捞出来**」。

---

## 四、欠着的活

| 事 | 状态 |
|---|---|
| **`引擎-抓取.js`**（不问版式、按长相认五样：品名/数量/点位/规格/备注） | 写完了，**没接进页面**。跑真表格出得来 57 份。这是「并成一条路」的方向 |
| 那 57 份接进表格路、替掉「问 AI 哪列是什么」 | **下一步就是这个**，等老板点头 |
| 线上部署 | 网站落后一周多，连不上服务器推不了 |
| 斤转板 | 8/2 vs 8/4 两条规矩打架，**等问工厂，给答复前一行不许改** |

## 五、今天（8/15–16）已经做完的

- 修好本地测试台的 `/api` 转发（见上）
- 全系统盘点：143 个文件，**搬走 24 个确定废物** → `桌面\食品系统-旧料\`（没删，随时拿回来）
- 搬完跑 `node 全部检查.js` → **29 项全绿**，开单台没受影响
- 权限弹窗设置写死

## 六、还没处理、等老板拍板的清理项

| | 大小 | 建议 |
|---|---|---|
| `.放大测试` 文件夹 | **14 MB** | 搬走（git 早就忽略它，纯占硬盘） |
| `会话回溯.md` + 摘要 | 1.8 MB | 搬走 |
| `配送开单台-新版UI.html` + `备份-0809-1436` | 886 KB | 旧界面版本，还看吗 |
| `8.10尝.xlsx` | 459 KB | **不知道是什么，没敢碰** |
| 旧规划文档 5 个（待办/排期/体系梳理/功能选型） | 103 KB | 还看吗 |
| 手动小工具 24 个（观麦爬虫/补商户名/价格对账） | 122 KB | 以后还用吗 |

## 七、两份记忆在各走各的（要处理）

`项目\记忆\`（38 个，git 跟踪）和自动记忆 `~/.claude/projects/c--Users----Desktop-----/memory\`（42 个）
是**两份拷贝**，其中 **6 个同名内容对不上**：
`MEMORY.md`、`ai-recognition-cost-plan.md`、`ask-before-touching-kaidan.md`、
`deliver-local-not-artifact.md`、`deployed-site-and-server.md`、`spot-book-copy-paste-only.md`

这正是老板最恨的「第二份真相」，得合成一份。**还没动手。**

---

## 八、说话方式

- 说人话，说简单点。他会直接说「说简单点」「什么意思」
- 表格/结果写文件，别在对话里打印整张表
- **动开单台必须先问**
- 反常的拎出来问，别改全局规则
- 先对终极底层逻辑、再挖根子，别打补丁
