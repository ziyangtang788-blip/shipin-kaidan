# 问题总捋（2026-08-04 重新整理）

老板：「他现在的问题其实不多，就总共就是那些，但是来回错，来回错。
不要去猜一些东西，猜着做有什么问题……**一定要让他读懂表格，这个很关键**。」

---

## 一、一句话：所有的错，都是同一件事

> **格子里的东西没读全。**

不是十几个毛病，是**一个**毛病的十几种样子。下面每一条都能归到它。

| 表现 | 其实是 |
|---|---|
| 总数量格写着 `8.7斤` / `+1斤`，只读到 8.7 | 一个格子里有两行，只读了一行 |
| 分布里 `(A859南站亚朵#员工)1斤` 整段没了 | 一个格子里有七段，只读了六段 |
| `A827 领逸` 的 2 斤读成 `?` | 格子里的字没看清 |
| 分布的键写成 `数`，读取只认 `数量` | 格子读进来了，交接的时候掉了 |
| 点位 `号`/`名` 读不到 | 同上 |

**所以主线只有一条：把表读懂、读全。** 别的都是它的下游。

---

## 二、今天实际发生过的错（只列有证据的）

⚠ 没有证据的一律不写。我今天推测过的东西，第五节统一作废。

| # | 现象 | 证据 | 状态 |
|---|---|---|---|
| 1 | 分布的单字键（`数`/`位`/`注`）读不到 → **整行变 0 品，还打 ✅** | 同一份格子表换键名，0 品 → 14 品 | **已修**，测试钉住 |
| 2 | 点位 `号`/`名` 读不到 → 点位码变空 | 同上那份格子表 | **已修** |
| 3 | `A859 南站亚朵 1斤` **整段漏读** | 原图有这一段，系统输出没有 | **没查**（缺那张图的文件） |
| 4 | `A827 领逸 2斤` 读成 `?` | 原图看得清，系统标了看不清 | **没查** |
| 5 | 小豆卜总数格 `8.7斤`+`+1斤` **只读了 8.7** | 观麦配送单确认是 **9.7 斤** | **没修**（识别层） |
| 6 | 有校验码就不回头核 → 对不上也不复查 | 江云 13≠14 却没触发复核 | **已修** |
| 7 | 点位册拿旧名**覆盖**这次读到的名 | 代码 `g.code=r.该写` | **已修** |
| 8 | 拖文件没反应 | 老板在 **旧版 v20260804f** 上遇到 | **没查**，要复现 |

**3、4、5 是同一类：格子里的东西没读全。5 有观麦白纸黑字作证，是最硬的一条。**

---

## 三、按老板说的流程，应该怎么走

> 「一定要让他读懂表格……对应的去填，填完再反过来，有问题的地方再去再读一遍，
> 读错总数看对不对。对不对就确定嘛，让客户先把有问题的先确定，再把没问题的再列出来。」

```
① 读懂表        看清这张表怎么摆的：哪列是品名、哪列是总数、分布怎么串
                ⚠ 一个格子里有几行、有加法，全都要读进来
        ↓
② 对应地填      填进固定的格子表，一个萝卜一个坑，不许挤、不许漏
        ↓
③ 回头核        只核有问题的那几处：总数对不上的行、标了 ? 的段
                （没问题的一个字都不重读 —— 省钱也更准）
        ↓
④ 总数对不对    对上了 → 过
                对不上 → 是各段读错了？还是总数本身读错了？（两个方向都要能修）
        ↓
⑤ 人来确定      先把有问题的几行摆给人确认
                确认完，没问题的列出来 → 出单
```

**押金那几行不用管**（老板 8/4）。

---

## 四、每一步现在缺什么

| 步 | 现在的状态 | 缺什么 |
|---|---|---|
| ① 读懂表 | 有「表怎么读的」自查，但**格子内部多行/加法读不全**（错误 5） | **提示词要交代：一个格子里有几行、有 `+`，全部读进来、加起来** |
| ② 填 | 格子表机制在，键名的坑已堵 | — |
| ③ 回头核 | **已改成**：对不上 / 有 ? → 自动只核那几行 | 老板要求**去掉「整单都回头核」那个勾**（还没做） |
| ④ 总数对不对 | 只能说「总数填错了」（赖客户） | **要能说「总数是我们读错了」并直接订正**（还没做） |
| ⑤ 人来确定 | 有问题的会摆出来；补齐了警告会消 | 「先确认有问题的、再列没问题的」这个**次序还没做** |

---

## 五、我猜过的东西 —— 全部作废

老板：「不要去猜一些东西，猜着做有什么问题。」

以下是我今天**没有证据就写下来**的，一律作废，不许当结论用：

1. ~~攸县香干 30片 = 4.25 斤~~ —— 我从两张图相减推的，没有依据
2. ~~「明瑞-1盒」会变成独立商品「尝元山水豆腐」~~ —— 同上
3. ~~A859 之所以漏，是因为号和名之间没空格~~ —— 只是巧合，没验证过
4. ~~「拖文件没反应」是我改坏的~~ —— 老板用的是旧版，跟今天的改动无关

**规矩（§0.2）：反常的拎出来问，不要猜着改规则。今天我犯了这条。**

---

## 六、我需要老板定的（不猜，等回话）

1. **押金**确认不管 —— 那配送单上那 4 行押金，识别时是直接跳过，还是照读但不算钱？
2. **④ 那一步**：总数读错了要能订正 —— 是让人直接改那个数，还是点一下「重读这一行的总数」？
3. **⑤ 的次序**：「先确认有问题的、再列没问题的」——
   是要把有问题的排到最上面，还是要分成两块（上面"待确认"、下面"没问题"）？
4. **拖文件没反应**：拖的是什么文件（图片 / Excel / PDF）？页面右下角版本号是几？
5. **A859 那张 8/4 的图**：存到哪儿了，给个路径，我才能查错误 3、4。

---

## 七、动手顺序（等老板点头）

1. **①：让它读懂格子里的多行和加法** ← 主线，错误 5 就是它，错误 3、4 大概率也是
2. **④：总数能订正**
3. **③：去掉整单回头核的勾**
4. **⑤：先摆有问题的**
5. 拖文件那个故障，等复现信息

**在①做完之前，别的都是补丁。**
