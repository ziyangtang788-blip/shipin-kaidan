# 家底 —— 项目里一共 174 个文件，一行一句

2026-08-16 盘的。**这张纸只是摊开给你看，一个字都没改、一个文件都没动。**

分六堆：① 在跑的 ② 考试卷 ③ 一次性小工具 ④ 数据 ⑤ 文档 ⑥ 可能是废料

---

## ① 在跑的（18 个）——  动这些才会影响开单台

页面 842–858 行实打实加载的就这 17 个 + 页面本身。**别的都影响不到开单台。**

| 文件 | 干什么的 |
|---|---|
| `配送开单台.html` | **主页面**，350 KB，所有界面和流程都在里面 |
| `数据-价格库.js` | 428 KB，谁家什么货多少钱 |
| `对照-预置.js` | 客户怎么叫 → 我们叫什么，出厂就带的那批 |
| `数据-常用规格.js` | 常见规格写法 |
| `数据-换算.js` | 斤 / 板 / 盒 怎么换 |
| `数据-商户名.js` | 商户名对照 |
| `数据-切法规矩.js` | 切块、切片这类做法 |
| `引擎-读文件.js` | 拖进来的 Excel / Word / PDF 怎么打开 |
| `引擎-解析.js` | 90 KB，**断句**——一坨字切成一段段，磨了两星期那个 |
| `引擎-读结构.js` | 52 KB，表格骨架 |
| `引擎-认表.js` | 52 KB，**问 AI「哪列是什么」** ← 你说对不上逻辑的就是它 |
| `引擎-匹配.js` | 48 KB，认货认价 |
| `引擎-断句规矩.js` | 断句的规矩 |
| `引擎-点位册.js` | 点位号 → 点位名（只照搬，没权威） |
| `引擎-OCR.js` | 图片认字 |
| `新-按内容认单.js` | 20 KB，8/12 写的「按内容认」，**已经接进页面了** |
| `导出-观麦.js` | 导成观麦能吃的格式 |
| `密钥-本机.js` | 密钥，**绝不能上传** |

**外加两个后台**（不在页面里，但要跑）：
- `后端-server.js` —— 服务器，订单记录 / 单号 / 图片识别代理
- `本地开一个网站.js` —— 本地测试用，8123

---

## ② 考试卷（33 个）—— 改完跑一遍看有没有搞坏

一条命令全跑完：`node 全部检查.js`

- `测试-*.js` 25 个：解析引擎 / 认表 / 匹配 / 读结构 / 读文件 / 点位册 / 学习 / 斤转板 / 搜索框 / 单位不许串 / 页面能跑起来 …
- `体检-*.js` 7 个：会不会认错 / 解析漏没漏 / 斤还是板 / 学过的还对吗 / 线上跟本地一样吗 …
- `全部检查.js` 1 个：把上面全跑一遍

---

## ③ 一次性小工具（26 个）—— 当时为了查一件事写的，平时用不着

- **观麦相关 4 个**：爬报价单价格 / 爬订单 / 爬一天 / 导入字段清单
- **查东西的 8 个**：查222 / 查门店串没串 / 对词 / 价格对账 / 看客户品 / 客户档案 / 补商户名（3 个文件）
- **试东西的 8 个**：试OCR / 试读结构 / 试放大 / 试放大批量 / 试一单 / 试真流程 / 验Excel读得对不对 / 试-按内容认单
- **跑批的 4 个**：批量跑真单 / 跑真单-点名 / 生成价格库.ps1 / 生成客户收集名单
- **分析的 2 个**：该先补哪家 / 数据盘点
- `batch_backfill.py` —— 走 Batch API 打五折那个，**没用过**

---

## ④ 数据（14 个）

| | 说明 |
|---|---|
| `本地数据\orders\*.jsonl` | **真订单记录**，450 KB，别删 |
| `本地数据\overlay.json` | **学过的 905 条客户叫法**，120 KB，最值钱的东西 |
| `本地数据\seq.json` | 单号排到哪了 |
| `本地数据\orders.bak-0809\` | 8/9 的备份 |
| `覆盖层-线上.json` | 线上那份覆盖层 |
| `基线-49份-*.json` 2 个 | 8/4 跑的基线，留着做对比 |
| `验AI指认\` 7 个 | 8/12 验证跑出来的日志和结果 |
| `批量跑-*.json` 2 个 | 空的 / 几乎空的 |
| `数据盘点.json`、`价格对账-差异.csv`、`缺价清单*.csv`、`客户名单*.csv` | 早期整理出来的表 |

---

## ⑤ 文档（15 个）

**要看的：**
- `CLAUDE.md` —— 工作规矩，每次开会话都读
- `说明-规矩总表.md` —— **唯一权威**，523 行
- `记忆\` 38 个 —— 你拍板的原话和为什么
- `交接-2026-08-16.md` —— 昨天的交接
- `交接-开单台全貌.md` —— 开单台全貌

**旧的，还看不看你说：**
- `说明-老板定的规矩清单.md`、`说明-规则冲突清单.md`、`说明-问题总捋.md`
- `后端设计.md`、`观麦导入-进展.md`
- `食品配送·出单录单系统.txt`、`要向工厂索取的下单表清单.txt`

---

## ⑥ 可能是废料（21 个，2.5 MB）—— 等你点头才搬

| | 大小 | 我的判断 |
|---|---|---|
| `会话回溯.md` + `会话回溯—摘要.md` | **1.8 MB** | 聊天记录，占地方，建议搬走留档 |
| `配送开单台-新版UI.html` | 308 KB | 旧界面版本 |
| `备份-0809-1436\` 3 个 | 863 KB | 8/9 的备份，现在有 git 了 |
| `8.10尝.xlsx` | 459 KB | **不知道是什么，没敢碰** |
| `UI-五个版本.html`、`UI-下拉五个版本.html`、`搬家.html`、`设计-识别完那一片.html` | 53 KB | 界面草稿 |
| `待办-大白话版.html`、`待办-总清单.html`、`排期-能做的按优先级.html`、`体系梳理.html`、`功能选型清单.html` | 103 KB | 早期规划，都过时了 |
| `本地预览.js` + `本地预览-双击这个.bat` | 7 KB | 被 `本地开一个网站.js` 取代了 |
| `江云.png` | 33 KB | 一张测试用的单 |
| `后端-装到服务器.sh` | 1 KB | 装服务器用的 |

---

## 三件要你拍板的事

**1. 有两份「按内容认单」**
`新-按内容认单.js`（已接进页面）和 `试-按内容认单.js`（没接）。
这就是规矩里说的「第二份真相」。要不要把 `试-` 那份搬走？

**2. `引擎-抓取.js`（31 KB）没接进页面**
按你 8/12 说的「不问版式直接抓」写的，写完了，一行都没用上。
留着 / 接进去 / 搬走 —— 你说。

**3. 记忆存了两份**
`项目\记忆\` 38 个，和 Claude 自己那份 42 个，**6 个同名的内容对不上**。
也是「第二份真相」。要不要合成一份？
